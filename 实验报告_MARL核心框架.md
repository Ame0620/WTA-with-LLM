# 实验报告（终版 v2）：DN-WTA marl 改进算法（批化训练 + 信用摊派 + BC 热启动）

> 版本：v2.0（2026-09-08）。本报告覆盖并替代 v1.1（2026-09-03）：v1.1 记录了 marl
> 六模块框架（M1–M6）交付与首轮训练（E14，test 泄漏率 0.9009，未追平 greedy
> 0.7913）；本版记录其后的完整改进方案（诊断 A0 → 批化 A1 → 预算 A2 → 信用摊派
> A3 → BC 热启动 A4 → c_invalid 扫描 E16 → PO-CPLEX 基线 E18 → 终评 E19），以及
> **终评阶段发现并修复的环境全局弹药池同拍竞态缺陷**（§4——本版最重要的工程
> 事件，曾系统性污染全部学习策略臂的成绩口径）。
> **数字来源约定**：一切结果数字的唯一来源为 `output/` 实测产物
> （`family_report.json` / `train_summary.json` / `bench.md` / `diag_report.md`）；
> 除明确标注"修复前"的对照列外，主表一律采用**环境修复后**复测值。

---

## 1. 问题设定与实验系统（与 v1.1 一致，补充仲裁语义）

### 1.1 场景与动力学（数据集 `dn-data-v3` 锁定参数）

| 项 | 值 |
|---|---|
| 平台/目标/步数 | m=3 平台（=智能体数），n=50 目标，K=10 决策步 |
| 弹药 | μ=6/平台，**全局共享池 18 发**，整局不补给，每平台每步至多 1 发；**同拍多平台发射按平台序仲裁，池尽转待机（v2.0 修复语义，见 §4）** |
| 时间 | dt=10 s/步；目标每步逼近 δ_d=3 km；拦截弹速 v_m=1 km/s |
| 杀伤概率 | p_eff = min(0.95, p_ij·d0_ij/d_ij(t))，发射时锁定，弹着步伯努利结算 |
| 泄漏 | r_j(t)≤0 即突防泄漏（全额价值）；t=K 仍存活计期末泄漏 |
| 决策窗 | t ∈ [0, K−2] |
| 数据划分 | test=s01–s02、train=s03–s26、val=s27–s30（`MANIFEST.md` 锁定）；s01 总值 3942 / s02 3819；w 时序趋势 1.5，r0 配额 1,1,3 |

任务概述：目标分批到达，三平台共享 18 发弹药；每步各平台独立决定对哪个目标发射
或保持待机；拦截弹经数步飞行后按发射时锁定的概率伯努利结算；目标突防时间耗尽即
价值泄漏。优化目标为最小化泄漏价值总和。

### 1.2 信息边界与策略族

每平台每步可见：共享态势（已出现目标状态、全局池水位）、平台私有量（自身距离/
p_ij/自身在途弹）、实例公共先验。不可见：未来到达序列、友军在途弹去向、环境
内部真值。marl 执行路径严格受限于此边界（专利文档 §7）。

| 策略 | 定位 | 信息口径 |
|---|---|---|
| `none` | 无防御下界 | 不动作 |
| `greedy` | 分布式局部贪心 | 仅 §7 观测 |
| `cplex` | 集中式短视最优（gap 分母） | 全知（参考解源） |
| `pocplex`（v2 新增） | §7 观测下的逐步 CPLEX（belief 重建） | §7 + belief |
| `marl` | 本项目 CTDE 学习策略（M1–M6，28 482 参数） | §7 + 公共先验 + 自身记忆 |

### 1.3 评估协议

终评 test × 30 MC seeds（42–71）全策略同协议；指标：泄漏率（核心）、逐步 gap、
无效交战率、弹药效率（毁伤值/发）、时延 p50/p90、发射数、毁伤价值、**仲裁拦截数
pool_blocked（v2.0 新增）**；RL 专属：⑨收敛曲线 ⑩泛化性 D=train−test ⑪训练成本。
result_hash 逐位复现。

---

## 2. marl 框架本体（v1.1 已交付，存目）

六模块与 v1.1 §2 完全一致：M1 感知重构（λ̂ 友军火力承诺度递推估计）、M2 集合
注意力编码（置换不变）、M3 决策头（交战打分 + 弃射位）、M4 合法性屏蔽、M5 奖励
与信用（事件流 + 势塑形 + 击杀归因）、M6 CTDE 训练（MAPPO 式 PPO + COMA 式反事实
基线）。参数量 28 482 不变，五模块 selftest 全部保持 PASS。v2 全部改动在 M6 训练
链与奖励信用构造（§3），**actor 执行路径零改动**——部署形态与 v1.1 完全相同。

---

## 3. 改进方案：诊断驱动的 A0–A4

### 3.0 A0 只读诊断（`output/e15_diag/diag_report.md`）

1. **λ̂ 质量偏低但方向可用**：对他人平台在途占用 Pearson 0.180 / MAE 0.076
   （27 000 样本）；age 0–1 桶高估约 2×（λ̂ 0.116 vs 真值 0.063）。→ 保留 M1。
2. **e14 曲线判定 PLATEAU**：best val 0.8943 @ iter 60，此后 20 评估点无改善，
   后半段斜率 −0.000114/iter——早停合理，瓶颈在训练效率与信用信号。
3. **弹药形态与整形分解**：e14 以 3,0,3,0,3,0,3,3,3 整除齐射打光 18 发（无效
   交战率 0.1042）；720 发结算分解 kill 194 / miss 451 / invalid 75——击杀信号
   稀疏（27%）且全额归因到 (t_fire, i) 方差大，**信用稀疏是首要瓶颈**；miss
   占 62.6% 提示多打同一目标的协同不足。

路线：A1 提速 → A2 拉长 → A3 信用重整 → A4 热启动；另触发 Φ 符号备选（§3.5）。

### 3.1 A1 PPO 批化（等价性门禁 PASS + ~170×，`output/e15_smoke/bench.md`）

PPO 更新逐样本前向改为 ragged 批式（`forward_batch`，pad 槽 −inf）。同一权重
快照 + 同批 flat（n=216，randperm 对齐）单次 `ppo_update` 等价性：

| 指标 | loop | batched | 相对误差 |
|---|---:|---:|---:|
| policy_loss | -0.02108761 | -0.02108762 | 1.77e-07 |
| v_loss | 0.27447083 | 0.27447082 | 4.00e-08 |
| max abs ΔW_actor / ΔW_critic | — | — | 2.94e-06 / 2.98e-08 |

两处深坑修复：① key_padding_mask 拼接序必须 `[~pad_mask, zeros(B,2)]`（target
token 在前）；② 批式熵项未选中分支反向 `0×(−inf)=NaN` 污染 autograd，须先
`lp_safe = where(fin, lp, 0)` 再乘。加速：e14（MPS 逐样本）60 s/iter → CPU 批式
**0.35 s/iter（~170×）**；e15 起正式训练全部 `--device cpu`。

### 3.2 A2 训练预算放宽

episodes/iter 32→128，eval 间隔 20→25 iter，patience 20→60 评估点，上限 3000
iters；同墙钟采样量放大 26×（132k → 3.46M steps）。

### 3.3 A3 击杀信用摊派（`marl/reward.py`，selftest 全 PASS）

v1.1 击杀 +w/total 全额记给 (t_fire, i)——同目标齐射时命中者独占信用、方差大。
改为**按在途份额摊派**：目标 j 击杀时其全部在途弹按锁定概率 p_k 归一分享
（credit_k = w/total · p_k/Σp）。selftest：质量守恒、键集合法、重放对账 <1e-9。

### 3.4 A4 BC 热启动（`marl/bc_pretrain.py`，greedy 教师）

教师 = greedy（§7 观测内），train split rollout 1 482 episodes → 40 014 个学生
视角样本；**标签为环境修正后的教师动作**（wrapper 复刻 env.run 非法翻转后再
标注）。离线：argmax 一致率 train 0.9220 / holdout 0.8900（CE 0.3274）。在线
门禁：kills/ep 教师 10.45 = BC 10.45，**比率 1.000（门槛 ≥0.95，PASS）**。
`bc_init.pt` 以 `--init-blend` 插值两档：0.1（bc01）/ 1.0（bc10）。

### 3.5 E16 扫描与 Φ 符号对照

- **c_invalid ∈ {0.005, 0.01, 0.03, 0.05} 四档**（c005/c010/c030/c050），其余
  同 e15；c010 与 e15 构成 A3 净对照（唯一差异摊派）。
- **pos 对照**：c010 配置下势函数 Φ 符号翻转。val 0.6418 vs 0.5963、test
  0.6464 vs 0.6149（修复后口径），**备选被数据否决**，维持历史 Φ 符号。

### 3.6 E18：PO-CPLEX 基线（`tests/test_pocplex_boundary.py` 全 PASS）

`POCplexPolicy`：每步从 §7 观测重建 belief（观测模型含 d/p/age/r/alive/pool 全
要素 → belief **精确**重建联合状态），交既有逐步 CPLEX 求解；信息边界由
`BoundaryProxy` 硬约束（单测拦截越界属性访问）。**实测 gap≡0.0000 且
result_hash 与全知 cplex 逐位一致**：本观测模型下 PO-CPLEX ≡ 全知 myopic-CPLEX
——信息边界对中心化短视求解零损失，pocplex（leak 0.7085±0.0343，p50 0.78 s）
构成学习策略"同信息、同量级计算"的公平上界。

---

## 4. 环境全局池同拍竞态：发现、修复与影响量化（v2.0 核心工程事件）

### 4.1 缺陷

E19 复核发现 e16_c010 `shots_total` 均值 19.47 > 池上限 μ×m=18，逐 run 出现
`pool_end=−1`。定位 `dwta/dn_env.py::run` 发射段：**合法性检查循环与开火循环
分离**——同拍多平台发射时检查循环读到的都是开火前同一池水位；池=1 且 ≥2 平台
同时合法发射时全部通过，开火逐个扣减使池穿底，等效每局多 0.5–1.6 发弹药。
**触发面**：e15/e16/e17 全部学习臂（训练亦在缺陷环境进行）；e14 与三基线不
触发——整除齐射节奏（池每次 −3 归零，无残余 1–2 发竞态窗口），故 v1.1 与
基线数值从未暴露。

### 4.2 修复与三层验证

开火循环按平台升序**逐发复查池**：池 ≤0 该平台本拍转待机，记入新增
`pool_blocked`（资源仲裁，与非法动作分列）；result_hash 指纹口径不含新字段，
新旧可逐位比对。验证：① 极限测试（全平台每步齐射）池不变量成立（18 发整、
pool_end=0）；② **回归**：greedy 0.7913±0.0095 / cplex 0.7085±0.0343（hash
逐位一致）/ pocplex 0.7085 / e14 0.9009——对从未超发的策略零行为改变；
③ 学习臂复测见 §4.3。

### 4.3 影响量化（test × 30 seeds，`output/fix_marl/` vs `output/e19_eval/`）

| 臂 | 修复前 leak（发数） | 修复后 leak（发数） | 拦截/局 | 回吐 |
|---|---|---|---:|---|
| e14 | 0.9009（18.00） | 0.9009（18.00） | 0.00 | 0 |
| e15 | 0.6194（19.58） | 0.6558（18.00） | 1.58 | +6.0pp |
| c005 | 0.5785（18.57） | 0.5993（18.00） | 0.57 | +2.1pp |
| c010 | 0.5546（19.47） | 0.6149（18.00） | 1.47 | +6.0pp |
| c030 | 0.5729（18.90） | 0.6095（18.00） | 0.90 | +3.7pp |
| c050 | 0.5967（18.58） | 0.6092（17.95） | 0.63 | +1.3pp |
| pos | 0.6302（18.52） | 0.6464（18.00） | 0.52 | +1.6pp |
| bc01 | 0.5684（19.33） | 0.6199（18.00） | 1.33 | +5.2pp |
| bc10 | 0.6199（18.53） | 0.6401（18.00） | 0.53 | +2.0pp |

结论：① 修复前"超 PO-CPLEX 21.7%"含弹药水分，如实回落（§5.2）；② **臂间排序
改变**——修复前 c010 第一，修复后 **c005 第一**（c010 超发最狠 1.47/局），选档
由 c\*=0.01 改判 **c\*=0.005**；③ 训练亦带偏，E20 干净重训验证恢复空间（§5.6）。

---

## 5. 实验结果（除注明外均为环境修复后口径）

### 5.1 训练过程汇总（`train_summary.json` 实测）

| 臂 | 配置差异（相对 e15） | best val | iters（停止） | env_steps | 墙钟 |
|---|---|---:|---|---:|---:|
| e14（v1.1 参照） | MPS 逐样本，32 ep/iter | 0.8943 | 460（早停） | 132 480 | 1.64 h |
| e15 | A1 批化 + A2 预算 | 0.6226 | 3000（打满） | 3 456 000 | 0.93 h |
| c005 | + A3 摊派 c=0.005 | 0.5896 | 3000（打满） | 3 456 000 | 1.46 h |
| c010 | + A3 摊派 c=0.01 | 0.5963 | 2525（早停） | 2 908 800 | 1.28 h |
| c030 | + A3 摊派 c=0.03 | 0.5948 | 1750（早停） | 2 016 000 | 0.94 h |
| c050 | + A3 摊派 c=0.05 | 0.6019 | 2775（早停） | 3 196 800 | 1.37 h |
| pos | c010 但 Φ 翻转 | 0.6418 | 1950（早停） | 2 246 400 | 0.59 h |
| bc01 | c010 + BC blend 0.1 | **0.5837** | 2250（早停） | 2 592 000 | 1.28 h |
| bc10 | c010 + BC blend 1.0 | 0.5917 | 3000（打满） | 3 456 000 | 1.52 h |

A3 净对照（e15 vs c010，唯一差异摊派）：val −4.2%、test −6.2%。四档扫描与 BC
两档均收敛，无发散或 NaN。

### 5.2 test 终评主表（`output/fix_marl/`、`output/fix_regress/`、`output/e18_eval/`）

| 指标 | none | greedy | cplex | pocplex | e14 | e15 | **c005** | c010 | c030 | c050 | bc01 | bc10 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **泄漏率** | 1.0000 | 0.7913 | 0.7085 | 0.7085 | 0.9009 | 0.6558 | **0.5993** | 0.6149 | 0.6095 | 0.6092 | 0.6199 | 0.6401 |
| ±std | ±0 | ±0.0095 | ±0.0343 | ±0.0343 | ±0.0295 | ±0.0161 | ±0.0319 | ±0.0386 | ±0.0368 | ±0.0471 | ±0.0530 | ±0.0328 |
| worst 实例 | 1.0000 | 0.8008 | 0.7428 | 0.7428 | 0.9304 | 0.6719 | 0.6312 | 0.6535 | 0.6463 | 0.6562 | 0.6729 | 0.6729 |
| gap | n/a | 0.2768 | 0 | 0 | 0.7032 | 0.2354 | 0.3560 | 0.3779 | 0.3769 | 0.4015 | 0.3880 | 0.4033 |
| 无效交战率 | 0 | 0.3472 | 0.0963 | 0.0963 | 0.1130 | 0.1611 | 0.1269 | 0.1509 | 0.1296 | **0.1052** | 0.1611 | 0.1806 |
| 弹药效率 | n/a | 46.7 | 67.9 | 67.9 | 21.8 | 72.0 | 84.0 | 82.7 | **88.5** | 84.6 | 80.9 | 76.9 |
| 发射数 | 0 | 18 | 18 | 18 | 18 | 18 | 18 | 18 | 18 | 18 | 18 | 18 |
| 毁伤价值 | 0 | 809.2 | 1129.1 | 1129.1 | 386.4 | 1334.8 | **1552.8** | 1492.0 | 1513.0 | 1513.8 | 1471.8 | 1394.4 |
| 时延 p50 (s) | 0 | 0.44 | 0.38 | 0.78 | 0.46 | 0.40 | 0.40 | 0.42 | 0.43 | 0.43 | 0.41 | 0.41 |

（时延含逐步 CPLEX 参考解开销；MARL 纯前向 p50 ≈0.004 s，与 v1.1 相同。）

**主结论**：

1. **演进链**：e14 0.9009 → e15 0.6558（A1+A2，−27.2%）→ **c005 0.5993**
   （+A3，再 −8.6%），总改善 **33.4%**；
2. **学习策略显著超越同信息中心化最优**：c005 较 pocplex/cplex 0.7085 低
   **15.4%**（修复前口径 21.7%——方向与显著性不变，幅度如实回落）；较 greedy
   0.7913 低 24.3%；
3. **gap 悖论**：MARL 泄漏率更低但 gap 更高（c005 0.356 vs greedy 0.277），且
   E18 证明信息边界内中心化短视最优就是 0.7085——**MARL 增益来自非 myopic 时序
   策略**（延迟发射/弹药储备），逐步短视参考不是其上界，此为学习策略价值的
   核心证据；
4. **弹药效率反超**：c005 单发期望毁伤 84.0 > cplex 67.9 > greedy 46.7
   （v1.1 时 marl 仅 21.8）——18 发全用但"打得准"；
5. c_invalid 扫描修复后 test 排序 c005 < c050 < c030 < c010：更强惩罚
   （c030/c050）压低无效率（0.13/0.11）且弹药效率最高（88.5），但泄漏率不再
   下降——**c\*=0.005 主指标最优**且无效率 0.127 达标（<0.15）；bc01 val 全场
   最佳（0.5837）但 test 0.6199 逊于 c005，BC 收益被摊派改进部分吸收。

主臂 c005 分实例（30 seeds 均值）：s01 leak 0.6312 / gap 0.4617 / invalid
0.1778 / 毁伤 1453.6（hash `9da4a980ea1a`）；s02 leak 0.5674 / gap 0.2503 /
invalid 0.0759 / 毁伤 1651.9（hash `dcf32c3f7d3e`）。

### 5.3 泛化性（⑩，`output/e19_generalization/`，修复后 `--no-ref` × 30 seeds）

| 臂 | train（24 实例） | val（4 实例） | test | D（train−test） |
|---|---|---|---|---|
| c005 | 0.6071±0.0346（worst 0.6549） | 0.6142±0.0487 | 0.5993 | **+0.0078** |
| c010 | 0.6080±0.0309（worst 0.6539） | 0.6144±0.0291 | 0.6149 | −0.0069 |

三 split 均值高度一致（|D| < 0.01，远小于 0.10 警戒线）——改进后的策略学的仍是
数据集共性规律而非实例记忆；无参考解负载下前向毫秒级。

### 5.4 行为特征读数（修复后）

- **弹药打满、节奏非整除**：各改进臂 18 发全用（c050 偶余弹 17.95），尾部零散
  补射被仲裁截留 0.5–1.6 发/局，但其期望收益已在 18 发约束内学得（leak 仍显著
  优于基线）；
- **协同估计成熟**：无效交战率 0.13（vs greedy 0.35）+ 弹药效率 84（vs 46.7）
  ——M1 λ̂ + M4 屏蔽对重复交战的抑制在充分训练量下显性化；
- **实例方差收敛**：c005 两实例 0.63/0.57（v1.1 e14 为 0.87/0.93）。

### 5.5 与 v1.1 结论的衔接

v1.1 三条差距归因（信用稀疏 / 预算不足 / 策略保守）分别由 A3+A4 / A1+A2 / 充分
训练对症缓解，泄漏率 0.9009 → 0.5993；v1.1 的部署特性结论（零非法动作、毫秒
前向、确定性复现）全部保持。

### 5.6 E20：修复后环境重训（进行中）

e15/e16/e17 均在缺陷环境训练（每局实际多 0.5–1.6 发）。E20 在干净环境重训两臂
（c005 / c010 配置，`output/e20_retrain/`）：截至撰写（iter 700/3000）val 已降至
0.6282 / 0.6472 且仍在下降段；终值以 `train_summary.json` 为准，本节留待补录。
预期：干净训练消除仲裁截断的 train-test 失配，test 应不劣于 §5.2 复测值。

---

## 6. 结论

1. **改进方案全部兑现且可归因**：A1 批化以 1.77e-07 等价门禁换 ~170× 加速；
   A2 采样量放大 26×；A3 净对照贡献 test −6.2%；A4 BC 在线复现比 1.000（val
   全场最佳 0.5837）；Φ 翻转与更强无效惩罚两个备选被数据否决。演进链
   e14 0.9009 → e15 0.6558 → c005 0.5993，总改善 **33.4%**。
2. **学习策略超越同信息中心化最优 15.4%**（c005 0.5993 vs pocplex 0.7085）；
   E18 证明 PO-CPLEX 与全知 myopic-CPLEX 逐位等价（gap≡0、hash 一致），差距
   不可能由信息或求解精度解释——非 myopic 时序策略是唯一剩余解释，gap 悖论
   （0.356 vs greedy 0.277）为行为学佐证。
3. **环境完整性事件**：发现并修复全局池同拍竞态；三基线与 e14 result_hash
   逐位回归通过，学习臂成绩按修复口径如实回落 1.3–6.0pp，选档由 c\*=0.01 改判
   c\*=0.005。教训：**多智能体共享资源环境的评估必须显式验证资源不变量**
   （shots ≤ μ·m、pool_end ≥ 0 已纳入冒烟自检）。
4. **交付形态不变**：28 482 参数、§7 边界内分散执行、纯前向毫秒级、三 split
   泛化 |D|<0.01、result_hash 逐位复现。

### 6.1 后续方向（优先级序）

1. E20 干净重训收尾（进行中）+ 多 seed 训练方差估计；
2. receding-horizon ≥2 步中心化基线：检验"非 myopic 时序优势"解释，将差距分解
   为视界损失与协同损失；
3. BC 教师换 PO-CPLEX（greedy 教师 0.79 的上限约束了 BC 初始化天花板）；
4. λ̂ 分龄校正消融（A0：age 0–1 桶高估 2×）。

---

## 7. 复现命令

```bash
PY=/opt/anaconda3/envs/wta/bin/python   # torch 2.13.0 / numpy 2.2.6

# 基线回归（修复后环境；hash 与 v1.1 锁定值逐位一致）
$PY experiments/dn_family_eval.py --split test --policy greedy  --seeds 30 --timelimit 30 --tmp-dir /tmp/rg --output output/fix_regress/greedy
$PY experiments/dn_family_eval.py --split test --policy cplex   --seeds 30 --timelimit 30 --tmp-dir /tmp/rc --output output/fix_regress/cplex
$PY experiments/dn_family_eval.py --split test --policy pocplex --seeds 30 --timelimit 30 --tmp-dir /tmp/rp --output output/fix_regress/pocplex

# A4 BC -> A3 改进训练（c*=0.005 主臂）
$PY marl/bc_pretrain.py --output output/e17_bc
$PY marl/train.py --device cpu --seed 0 --iters 3000 --eval-every 25 --patience 60 \
    --episodes-per-iter 128 --c-invalid 0.005 --output output/e16_scan/c005

# 终评 + 泛化快照（修复后）
$PY experiments/dn_family_eval.py --split test --policy marl --model output/e16_scan/c005/best.pt --seeds 30 --tmp-dir /tmp/m --output output/fix_marl/c005
$PY experiments/dn_family_eval.py --split train --policy marl --model output/e16_scan/c005/best.pt --seeds 30 --no-ref --tmp-dir /tmp/g --output output/e19_generalization/c005_train
$PY experiments/dn_family_eval.py --split val   --policy marl --model output/e16_scan/c005/best.pt --seeds 30 --no-ref --tmp-dir /tmp/g --output output/e19_generalization/c005_val

# E20 干净重训（c005 / c010 两臂）
$PY marl/train.py --device cpu --seed 0 --iters 3000 --eval-every 25 --patience 60 \
    --episodes-per-iter 128 --c-invalid 0.005 --output output/e20_retrain/c005

# 自检（等价性 / 摊派 / 边界）
$PY marl/network.py && $PY marl/reward.py --selftest --instances data/dn-data-v3/dn_3x50_K10_s01.txt
$PY tests/test_pocplex_boundary.py
```

---

## 附：产物索引

| 目录 | 内容 | 关键结果 |
|---|---|---|
| `output/e15_diag/` | A0 只读诊断 | e14 判定 PLATEAU；λ̂ Pearson 0.18；720 发 = kill 194/miss 451/invalid 75 |
| `output/e15_smoke/` | A1 批化门禁与基准 | 等价性 1.77e-07；0.35 s/iter（~170×） |
| `output/e15_train/` | e15（A1+A2） | best val 0.6226 / 3.46M steps / 0.93 h |
| `output/e16_scan/{c005,c010,c030,c050}/` | A3 扫描四档 | val 0.5896–0.6019；test（修复后）0.5993–0.6149 |
| `output/e16_pos/` | Φ 符号对照（否决） | val 0.6418 vs c010 0.5963 |
| `output/e17_bc/` + `marl/bc_pretrain.py` | A4 BC 教师 | 一致率 0.89/0.92；在线比率 1.000 |
| `output/e17_bc01/` `e17_bc10/` | BC 热启动两档 | val 0.5837（全场最佳）/ 0.5917 |
| `output/e18_eval/pocplex/` | E18 PO-CPLEX | leak 0.7085；gap≡0；hash 与 cplex 一致 |
| `output/e19_eval/` | E19 首轮终评（**修复前口径**） | 仅作 §4.3 对照保留 |
| `output/fix_regress/{greedy,cplex,pocplex}/` | 修复后基线回归 | 与 v1.1 锁定值逐位一致 |
| `output/fix_marl/`（9 臂） | 修复后学习臂复测 | 本报告主表数据源 |
| `output/e19_generalization/` | 修复后泛化快照 | c005：train 0.6071 / val 0.6142 / test 0.5993 |
| `output/e20_retrain/` | 修复后重训（进行中） | c005/c010；终值待补录 |
| `dwta/dn_env.py` | 环境仲裁修复 | 池逐发复查 + pool_blocked 计数；极限测试 PASS |
| `tests/test_pocplex_boundary.py` | 信息边界单测 | proxy 拦截 + 合法表面可达 |

