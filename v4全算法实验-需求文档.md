# DN-WTA v4 全算法实验 — 需求文档

> **文档版本**:v1.1(2026-09-18)
> **v1.1 变更**:预算分层重构——**EC-MAPPO(本文方法)保留完整档全流程(≤6h);其余全部算法压缩至 3h 全流程档**(训练墙钟 ≤2.5h + 评估 ≤0.5h),涉及 §0 D6、§2、§3.1.4、§3.1.6、§4.3、§6、§7、§8 V9''、§10、§11;报告口径须显式声明预算差异(见 §7.8)
> **上游依据**:`DN-WTA_v4_数据集说明.md`(v4 基准)、`实验报告_v3全算法总览.md`(算法族与协议基线)
> **交付目标**:在 v4 数据族(5 平台 × 100 目标)上完成 **9 算法** 全量终评(既有 7 算法平移 + 新增 MADDPG / GA),并产出《实验报告_v4全算法总览.md》
> **本文档性质**:需求设计文档(不含算法实现),作为后续开发、验收与报告撰写的唯一依据

---

## 0. 默认决策声明(待确认项的缺省取值)

以下决策点已按合理缺省值锁定,如需变更请在开发启动前提出,否则按本节执行:

| # | 决策点 | 缺省取值 | 理由 |
|---|---|---|---|
| D1 | MADDPG 定位 | **标准 MADDPG 基线**(进主表),仅做离散动作与环境最小适配,无 EC 定制模块 | 与"标准 MAPPO 作消融参照"地位对等,回答"off-policy 确定性策略梯度是否适配事件流 WTA" |
| D2 | GA 定位 | **滚动时域短视 GA**(进主表),与 CPLEX 同信息边界、同当拍目标 | 满足 §7 信息边界与在线约束,作为元启发式优化基线,gap 指标直接可比 |
| D3 | 算法范围 | **全量 9 算法**(v3 七算法全部 v4 终评 + MADDPG + GA) | 用户原始需求:v3 报告算法 + 新增两算法 |
| D4 | 报告结构 | 沿用 v3 总览结构 + 新增三节:v3↔v4 跨规模并表、MADDPG vs MAPPO 专题、GA vs CPLEX 专题 | v4 定位即"可扩展性验证基准",跨规模对比为必答题;两新算法需专门分析 |
| D5 | 可选扩展(本期不做) | EC-MADDPG(EC 模块跨算法族迁移验证)、全局进化 GA(离线上界参照) | 列入 §12 后续方向,主实验聚焦 |
| D6 | **预算分层(v1.1)** | **EC-MAPPO 完整档:全流程 ≤6h;其余 8 算法一律 3h 全流程档**(学习类训练墙钟 ≤2.5h;求解器/规则类评估本就远低于 3h) | 总计算预算有限,本文方法保留完整预算以验证跨规模保持性;基线定位为"资源受限参照",3h 档由墙钟上限硬保证,报告须声明该口径差异,不得将 3h 档数字解释为算法上限 |

---

## 1. 背景与目标

v4 是 v3 难度结构的**各向同性放大**(m 3→5、n 50→100,池/需求 0.67 精确平移),协议(30 MC seeds / split 划分 / seed 隔离)与指标体系完全一致。v3 结论的算法族层次:规则 0.85–0.89 > 值分解学习 0.83 > 短视优化 0.71 > 标准 CTDE 0.60 > EC-MAPPO 0.5963。

实验目标:

- **G1 主表**:9 算法在 v4 test(s01–s02 × 30 seeds)统一终评,产出与 v3 §5 同口径主表;
- **G2 跨规模对比**:9 算法 v3↔v4 归一化指标并表,量化各算法可扩展性衰减;
- **G3 新算法定位**:MADDPG 在学习族内位置(off-policy vs on-policy 对照);GA 相对 CPLEX 的求解质量–成本权衡;
- **G4 可复现性**:全部结果 result_hash 逐位复现,资源不变量零违例。

---

## 2. 算法总矩阵(9 算法)

| # | 算法 | 类别 | v3 状态 | v4 工作量 | 对比角色 |
|---|---|---|---|---|---|
| 1 | Random(p_hold=0.3) | 规则 | 已有 | **零代码**(切数据路径) | 随机下界 |
| 2 | Greedy-Nearest | 规则 | 已有 | **零代码** | 局部贪心参照 |
| 3 | Greedy-Threat | 规则 | 已有 | **零代码** | 局部贪心参照 |
| 4 | CPLEX | 优化(短视精确) | 探针已有(10 seeds) | 补 30 seeds 正式终评 | 短视最优参照(gap=0 源) |
| 5 | **GA(短视)** | 优化(元启发式) | **新增** | 新求解器 + 策略类 | 低成本逼近短视最优的可行性 |
| 6 | QMIX | 学习·值分解 | 已有 | v4 重训(**3h 档**)+ 适配 | 值分解失效假设的规模验证 |
| 7 | MAPPO | 学习·CTDE(on-policy) | 已有 | v4 重训(**3h 档**)+ 适配 | 标准 CTDE 消融参照 |
| 8 | **MADDPG** | 学习·CTDE(off-policy) | **新增** | 训练器 + 网络 + 策略类(**3h 档**) | off-policy 确定性策略对照 |
| 9 | EC-MAPPO(本文方法) | 学习·CTDE(on-policy+定制) | 已有 | v4 重训(**完整档 ≤6h**,§4.3) | 本文方法跨规模保持性 |

---

## 3. 新增算法需求详述

### 3.1 MADDPG(标准版,off-policy 学习基线)

#### 3.1.1 定位与红线

- **定位**:与标准 MAPPO 同等地位的"无问题定制"基线。唯一差异轴 = 训练范式(off-policy + 确定性策略梯度 + 中心化 Q critic vs on-policy + 随机策略 + 中心化 V critic);
- **红线(与既有学习基线完全一致)**:
  - 信息边界:执行侧仅 `env.get_observation(i,t)` + 公共先验(§7),训练侧 CTDE;
  - 参数量 ∈ [1e3, 1e5](复用 `assert_params` 检查);
  - 训练与评估零非法动作(复用 `marl/masking.py::feasible_mask`);
  - 评估期确定性执行(argmax),不触碰 `env.rng`(采样用独立 `torch.Generator`,照抄 `MarlPolicy` 隔离模式),保证 per-seed result_hash 确定;
  - 奖励口径与既有学习基线一致(复用 `marl/reward.py` 团队奖励,不得私改——否则与 MAPPO 的对照引入额外差异轴)。

#### 3.1.2 网络架构

- **Actor(确定性,缺省平台间共享)**:复用 `PoolMLPNet` 骨架(`baseline_net.py`,x∈R^8/q∈R^5/g∈R^3,池化 + per-target 评分,输出 [1+L] hold-first logits)——**与 MAPPO/QMIX 同构**,隔离"网络容量"差异轴;
- **Critic(中心化,每平台一个 Q_i)**:输入 = 全局状态行(与 `StateCritic` 同源,35 维:masked-mean pooled true targets 32 + global 3)**⊕ 联合动作嵌入**;
- **联合动作嵌入(置换不变设计,关键规格)**:平台 i 的动作 a_i 编码为所选目标特征 `x_j ∈ R^8`(hold → 零向量),联合动作 = m 个 8 维向量拼接 → v4 下 critic 输入 35 + 5×8 = **75 维**。禁止目标 id 的 one-hot(n=100 且置换敏感);
- **目标网络**:actor/critic 各一套 target 副本,soft update。

#### 3.1.3 离散动作适配(标准 MADDPG → 离散 WTA)

- 训练期动作采样:**Gumbel-Softmax 重参数化**(对 masked logits),温度 τ_exp 从 1.0 线性退火至 0.1;执行/评估期取 argmax;
- critic 的联合动作输入用 straight-through 硬 one-hot 样本;
- target 策略计算用 argmax 的 one-hot(标准离散 MADDPG 做法,抑制过估计)。

#### 3.1.4 训练算法规格(缺省超参,S0 smoke 后可调并记录)

| 项 | 缺省值 | 说明 |
|---|---|---|
| replay buffer | 1e5 联合 transition | 环形,存 (obs 特征, joint action, r, next obs, done) |
| batch size | 256 | |
| 折扣 γ | 与 `train_mappo.py` 对齐(读取同一常量) | 口径一致性红线 |
| soft update τ | 0.005 | |
| actor lr / critic lr | 1e-4 / 3e-4(Adam) | |
| 更新频率 | 每 episode 末批量更新(≤1 update/env step 上限) | off-policy 样本效率优势的体现点 |
| 探索 | Gumbel 温度退火(3.1.3),不用 OU 噪声(离散域无定义) | |
| 早停 | 复用 val split 周期评估 + patience(与 EC-MAPPO 机制同构) | |
| 训练预算 | **≤3h 全流程(v1.1 基线档)**:训练墙钟上限 2.5h + 终评/泛化评估;iters 上限 1200 × 24 ep/iter(墙钟到即停) | 先 0.5h smoke 实测速率定档;off-policy 样本效率优势在固定墙钟下体现 |

#### 3.1.5 接口与文件约定

| 交付物 | 路径 | 内容 |
|---|---|---|
| 网络 | `marl/maddpg_net.py` | 确定性 Actor(复用/继承 PoolMLPNet)+ CentralQCritic(joint-action 嵌入,维度由 m 推导,**不得硬编码 3**) |
| 训练器 | `marl/train_maddpg.py` | CLI 对齐 `train_mappo.py`(--device/--seed/--output/--patience...);checkpoint 含 `state_dict + feature_spec + params_count` |
| 执行适配器 | `marl/baseline_policy.py` 新增 `MADDPGPolicy` | 接口与 `PoolMLPPolicy`/`MarlPolicy` 一致:`act(env,t)->(actions,info)`、`reset_episode()`、t-rewind 检测 |
| 注册 | `dwta/dn_policies.py::build_policy` + `experiments/dn_family_eval.py` choices | 名称 **`maddpg`**,列入 `with_ref` 自动参考解集合(gap 指标) |

#### 3.1.6 MADDPG 验收标准(进入终评前置)

- **A1 正确性**:3 实例 × 5 seeds 冒烟,零非法动作、`shots ≤ 30`、`ammo_end ≥ 0`、result_hash 两次运行逐位一致;
- **A2 训练收敛**:val leak_rate 曲线单调改善且早停生效;若 3h 档预算内不收敛,如实报告并分析(失败本身是 off-policy 适配性证据,对标 v3 中 QMIX 的处理方式,不强行调参刷结果);
- **A3 公平性**:参数量进报告;奖励、观测特征、掩码与 MAPPO 逐项相同(代码级 diff 审查)。

---

### 3.2 GA(滚动时域短视,元启发式优化基线)

#### 3.2.1 定位与信息边界

- **定位**:每决策拍 t 用 GA 求解**当拍** WTA,与 `CplexPolicy` 完全相同的信息边界、状态读取方式与目标函数;验证"元启发式能否以更低部署成本逼近短视精确最优";
- **信息边界(硬约束)**:只读 `CplexPolicy` 同款联合可见状态(已出现目标、全局池水位、p_ij/d0_ij);不得读取未来到达、n、真值全知信息;
- **目标函数**:与 CPLEX 参考解同目标——当拍分配期望摧毁价值最大化(Σ p_ij·w_j),约束:每平台每步 ≤1 发、全局池水位、交战窗合法性。

#### 3.2.2 算法规格

| 项 | 缺省值 | 说明 |
|---|---|---|
| 编码 | 长度 m=5 整数染色体,基因 i ∈ {0}∪alive_ids(0=hold) | 短视单拍分配 |
| 适应度 | 当拍期望摧毁价值(预计算 w·p 表,查表 O(m));非法基因先修复再计分 | |
| 初始化 | 一半贪心种子(Greedy-Threat/Nearest 解 + 扰动),一半随机合法 | 降低收敛方差 |
| 选择 | 锈标赛 k=3 | |
| 交叉 | 均匀交叉 p=0.8 | |
| 变异 | 随机重置为合法等位基因 p=1/m | |
| 修复算子 | 超池/超射速/窗口外 → 强制 hold | 保证零非法动作 |
| 种群×代数 | 主档 **P=40, G=50**;敏感性 {20×25, 40×50, 80×100} | 主档 ≈2000 评估/拍 |
| 终止 | 达 G 代或连续 10 代最优无改进 | |
| 随机流 | GA 私有 `RandomState(policy_seed)`,与 env seed 完全隔离(照抄 `RandomPolicy` 模式) | result_hash 确定性前提 |

#### 3.2.3 时延约束

每拍 GA 墙钟 **P50 < 0.5s、P90 < 1s**(决策周期 10s 的 1/10;适应度纯查表,余量充足);时延与 CPLEX(0.39s)同量级——这是"GA 部署价值"论证的口径基础。

#### 3.2.4 接口与文件约定

| 交付物 | 路径 | 内容 |
|---|---|---|
| 求解核心 | `dwta/ga_solver.py` | 纯函数式 `solve_assign(visible_state, rng) -> assignment`;不依赖 torch/cplex |
| 策略类 | `dwta/dn_policies.py::GAPolicy` | `act(env,t)` 与 `CplexPolicy` 对齐;with_reference 时挂 CPLEX 参考解算 gap |
| 注册 | `build_policy` + `dn_family_eval.py` | 名称 **`ga`**,列入 `with_ref` 集合;新增 `--ga-pop/--ga-gen/--ga-seed` CLI 参数(带缺省) |

#### 3.2.5 GA 验收标准

- **B1 正确性**:3 实例 × 5 seeds,零非法、资源不变量、result_hash 复现;
- **B2 收敛质量**:test × 10 seeds 预检,GA 当拍分配价值 / CPLEX 参考价值 ≥ **0.95**(95% 分位);不达标先调 P/G 档位再进终评;
- **B3 时延**:P50/P90 达 3.2.3 红线;
- **B4 敏感性**:P×G 三档主指标漂移 ≤ 2pp(证明主档非运气)。

---

## 4. 既有 7 算法 v4 平移需求

### 4.1 零代码类(规则 + CPLEX 终评补跑)

`dn_env.py`/`dn_instance.py` 零修改已验证(v4 说明 V1')。仅需:

```bash
--data-dir data/dn-data-v4 --manifest data/dn-data-v4/MANIFEST.md
```

- CPLEX:探针 `e21_v4_cplex_probe`(10 seeds)已有,补 **30 seeds(42–71)正式口径**终评;
- 规则类:Random / Greedy-Nearest / Greedy-Threat 各 30 seeds 终评,参数沿用 v3(`--policy-seed` 等)。

### 4.2 学习类 v4 重训适配点(QMIX / MAPPO / EC-MAPPO)

| 文件 | 适配点 | 说明 |
|---|---|---|
| `marl/baseline_net.py` | `N_AGENTS = 3` 常量 → 由实例 m 推导或改 5 | 影响 QMIX mixer / critic 输入维度 |
| `marl/train.py` | CriticNet 联合动作维度 3·m → 15(act [B,15]) | v4 说明 §10 明示的训练侧适配点 |
| `marl/train_*.py` ×3 | 数据路径 → v4 train/val split;预算参数 | 见 4.3 |
| actor 侧 | **零改动** | MarlNet / PoolMLPNet 均为置换不变集合网络,对 m/n 天然泛化 |

### 4.3 训练预算(v1.1 分层口径)

**预算分层原则(D6)**:EC-MAPPO(本文方法)保留完整档以验证跨规模保持性;三个学习基线压缩至 **3h 全流程档**,且三者之间保持**同 iters×episodes 采样档 + 同墙钟上限**的公平性(差异只剩训练范式本身)。

| 算法 | 全流程预算 | 训练墙钟上限 | iters × ep/iter(采样档) | 早停 patience | 说明 |
|---|---|---|---|---|---|
| **EC-MAPPO** | **≤6h(完整档)** | 6h | 2500 × 96 | 60 eval 点 | 先 0.5h smoke 定档;本文方法完整预算 |
| MAPPO | **≤3h(基线档)** | 2.5h | 1200 × 24(=28,800 ep) | 12 eval 点 | 与 QMIX/MADDPG 同采样档同墙钟 |
| QMIX | **≤3h(基线档)** | 2.5h | 1200 × 24 | 12 eval 点 | 同上 |
| MADDPG | **≤3h(基线档)** | 2.5h | 1200 × 24 | 6 eval 点 | 见 3.1.4;墙钟驱动体现 off-policy 样本效率 |

配套缺省(训练器 CLI 缺省值与本表一致,见 §9 复现命令):

- 评估耗时核算(三基线):终评 30 seeds(挂 CPLEX 参考)+ 泛化三 split(`--no-ref`)合计 < 0.5h,故训练墙钟 2.5h + 评估 ≤ 3h 全流程;
- 求解器/规则类(CPLEX/GA/Random/Greedy×2)评估远低于 3h(CPLEX ≈ 数十分钟,GA 主档 ≈ 1 分钟,规则类秒级),**无预算风险,协议不缩水**(30 seeds 全量);
- 墙钟到限即停(训练器内建 WALL_LIMIT_SEC),任何"iters 未跑满即停"如实记入 train_summary.json 并在报告 §8 声明。

---

## 5. 统一评估协议(v4)

与 v3 完全一致(v4 说明 §8),锁定执行口径:

1. **正式对比**:test split s01–s02 × **30 MC seeds(42–71)**,per-instance mean±std + family 聚合;
2. **指标**:16 项主指标 + ⑰无效交战率 + ⑱弹药轨迹;主表呈现①泄漏率、④命中率、⑰无效率、期望摧毁价值、⑦时延 P50/P90、gap;**跨 v3/v4 对比仅用归一化指标**(泄漏率/命中率/无效率),绝对价值量(v4 ≈ 2×v3)不并表;
3. **参考解**:greedy 族 / 学习族 / GA 均挂 per-step CPLEX 参考(gap 口径);泛化快照用 `--no-ref`;
4. **泛化**:学习算法另报 train/val/test 三 split(30 seeds,`--no-ref`),偏差 |D| 警戒线 0.10;
5. **资源不变量**:每 run 校验 `shots_total ≤ 30`、`ammo_end ≥ 0`(V8' 冒烟自检,新算法同样纳入);
6. **复现**:family_report.json 记录 argv + source_md5;新增文件须加入 md5 清单(`marl/maddpg_net.py`、`marl/train_maddpg.py`、`dwta/ga_solver.py`、`dwta/dn_policies.py`)。

---

## 6. 实验执行计划(顺序与依赖)

| 阶段 | 内容 | 依赖 | 产出 |
|---|---|---|---|
| S0 冒烟 | MADDPG/GA 各 0.5h smoke(3 实例 × 5 seeds + 训练速率定档) | 无 | 速率报告、预算定档 |
| S1 求解器终评 | CPLEX(30 seeds 补全)+ GA 主档 + GA 敏感性三档 | GA 过 B1–B3 | `output/e3x_v4_{cplex,ga*}` |
| S2 规则终评 | Random / Greedy×2(30 seeds) | 无 | `output/e3x_v4_baselines` |
| S3 学习重训 | MAPPO / QMIX / MADDPG(各 **≤3h 全流程档**,可串行)→ EC-MAPPO(**完整档 ≤6h**) | S0 定档 | 各 `best.pt` + 训练曲线 |
| S4 学习终评 | 4 学习算法 test 30 seeds(含参考解)+ 三 split 泛化 | S3 | `output/e3x_v4_{ecmappo,mappo,qmix,maddpg}[_gen]` |
| S5 汇总报告 | 主表 + 跨规模并表 + 专题分析 + 回归核查 | S1–S4 | 《实验报告_v4全算法总览.md》 |

---

## 7. 《实验报告_v4全算法总览.md》章节需求

结构沿用 v3 总览,新增三节(标注"新增"):

1. 执行摘要:9 算法层次表 + 三条核心结论,须覆盖"v3 层次是否保持";
2. 实验基准:DN-WTA v4(规模参数、弹药压力平移 §9、结构性下界 ≈14%);
3. 评估协议与指标:同 v3 口径声明 + 归一化并表规则;
4. 对比算法:9 个;MADDPG/GA 各一节(机制概要、超参、对比角色);EC-MAPPO 定制模块照抄 v3 §4.2 并声明零改动;
5. test 终评主表(9 行,列同 v3 §5)+ 结果分析;
   - **5.5 v3↔v4 跨规模并表(新增)**:9 算法 × {v3 泄漏率, v4 泄漏率, Δpp, 衰减比 v4/v3},命中率/无效率同表;逐算法给可扩展性判词(如"QMIX 崩溃 / MAPPO 温和衰减 / EC-MAPPO 保持");
   - 分析必答:①层次保持性;②规模压力下各家族位移方向;③v4 拦截上限收窄(36%→30%)对结论的影响;
6. **MADDPG vs MAPPO 专题(新增)**:样本效率曲线(墙钟–性能)、最终性能、稳定性(30 seeds std)、off-policy 在事件流信用稀疏下的表现判词;**两算法同为 3h 档(同采样档同墙钟),对比公平性声明**;
7. **GA vs CPLEX 专题(新增)**:gap 分布(P50/P90)、时延对比、P×G 敏感性、部署价值结论(免 CPLEX 许可/纯 numpy 的工程意义);
8. 训练侧与泛化:4 学习算法参数量/训练步数/墙钟 + 三 split |D|;**须单列预算分层声明表(v1.1):EC-MAPPO 完整档 ≤6h vs 三基线 3h 档(1200×24 + 墙钟 2.5h),并显式警示"基线 3h 档数字为资源受限参照,不代表各范式收敛上限"**;
9. 支撑实验:资源不变量声明、v3 数字引用来源标注;
10. 关键发现汇总(编号列表,含规模可扩展性结论);
11. 复现命令与产物索引:算法名 ↔ `output/` 目录映射表(同 v3 §10 格式);
12. 后续方向:含 D5 的 EC-MADDPG、全局 GA、多训练种子方差估计。

**报告数字红线**:所有主表数字可溯源至 family_report.json;QMIX/MAPPO/EC-MAPPO 的 v3 数字直接引用 v3 报告并标注出处;守恒校验(泄漏价值 + 摧毁价值 ≈ total_value,量级 8732)逐算法过一遍(照抄 v3 §5 注的处理方式)。

---

## 8. 总验收标准(V'' 系列)

S5 完成后统一执行,全项 PASS 方可定稿报告:

| # | 验收项 | 方法 | 通过标准 |
|---|---|---|---|
| V1'' | 协议合规 | 抽查各算法 family_report.json params | test 2 实例 × seeds=30、seed_base=42、v4 data-dir |
| V2'' | 资源不变量 | 全部 9 算法 × 全部 runs | `shots ≤ 30`、`ammo_end ≥ 0`,零违例 |
| V3'' | 零非法动作 | run 记录 illegal_actions 求和 | 全部为 0 |
| V4'' | 结果可复现 | 任选 3 算法重跑 5 seeds 比对 | result_hash 逐位一致 |
| V5'' | MADDPG 正确性 | A1–A3(§3.1.6) | 全过 |
| V6'' | GA 收敛与时延 | B1–B4(§3.2.5) | 全过 |
| V7'' | 时延红线 | 全算法 latency_p90 | < 10s 决策周期(学习类目标 < 1s) |
| V8'' | 参数量红线 | 学习类 4 算法 | ∈ [1e3, 1e5] |
| V9'' | 训练预算(分层) | 学习类 4 算法墙钟(训练 + 评估) | EC-MAPPO ≤ 6h;MAPPO/QMIX/MADDPG 各 ≤ 3h |
| V10'' | 泛化偏差 | 学习类三 split | \|D\| < 0.10(警戒线) |
| V11'' | 跨规模口径 | 报告 §5.5 检查 | 仅归一化指标并表;v3 数字与 v3 报告一致 |
| V12'' | 守恒校验 | 泄漏价值 + 摧毁价值 vs total_value | 逐算法 9/9 通过 |

---

## 9. 交付物与目录规划

**代码(新增)**:

- `marl/maddpg_net.py` — MADDPG 网络(确定性 actor + 中心化 Q critic);
- `marl/train_maddpg.py` — MADDPG 训练器;
- `marl/baseline_policy.py` — 追加 `MADDPGPolicy`;
- `dwta/ga_solver.py` — GA 求解核心;
- `dwta/dn_policies.py` — 追加 `GAPolicy` + 注册两算法;
- `experiments/dn_family_eval.py` — `--policy` 增加 `ga`/`maddpg`、GA CLI 参数、source_md5 清单更新;
- `tests/test_v4_algorithms.py`(建议)— A1/B1 冒烟 + 资源不变量自动检查。

**实验产物(output/,目录名即报告 §11 索引)**:

| 目录 | 内容 |
|---|---|
| `output/e30_v4_cplex/` | CPLEX 30 seeds 正式终评 |
| `output/e31_v4_ga/{p40g50,p20g25,p80g100}/` | GA 主档 + 敏感性 |
| `output/e32_v4_baselines/{random,greedy_nearest,greedy_threat}/` | 规则类 |
| `output/e33_v4_{ecmappo,mappo,qmix,maddpg}/` | 学习类训练(checkpoint/曲线) |
| `output/e34_v4_{...}_eval/`、`e35_v4_{...}_gen/` | 学习类终评与泛化 |
| `logs/e3x_*.log` | 各阶段运行日志 |

**报告**:项目根目录《实验报告_v4全算法总览.md》(§7 结构)。

**复现命令模板**(报告 §11 用,口径对齐 v3):

```bash
PY=/opt/anaconda3/envs/wta/bin/python
V4="--data-dir data/dn-data-v4 --manifest data/dn-data-v4/MANIFEST.md"

# 求解器与规则类
$PY experiments/dn_family_eval.py --split test --policy cplex --seeds 30 \
    $V4 --output output/e30_v4_cplex
$PY experiments/dn_family_eval.py --split test --policy ga --ga-pop 40 --ga-gen 50 \
    --seeds 30 $V4 --output output/e31_v4_ga/p40g50

# 学习类基线(3h 档:训练器缺省 = 1200 iters × 24 ep + 墙钟 2.5h,无需显式传参)
$PY marl/train_mappo.py  --data-dir data/dn-data-v4 --output output/e33_v4_mappo
$PY marl/train_qmix.py   --algo qmix --data-dir data/dn-data-v4 --output output/e33_v4_qmix
$PY marl/train_maddpg.py --data-dir data/dn-data-v4 --output output/e33_v4_maddpg
# EC-MAPPO(完整档:2500 iters × 96 ep + patience 60)
$PY marl/train.py --data-dir data/dn-data-v4 --iters 2500 \
    --episodes-per-iter 96 --output output/e33_v4_ecmappo

# 终评 → 泛化(四算法同构;以 maddpg 为例)
$PY experiments/dn_family_eval.py --split test --policy maddpg \
    --model output/e33_v4_maddpg/best.pt --seeds 30 $V4 --output output/e34_v4_maddpg_eval
$PY experiments/dn_family_eval.py --split train --policy maddpg --no-ref \
    --model output/e33_v4_maddpg/best.pt --seeds 30 $V4 --output output/e35_v4_maddpg_gen
```

---

## 10. 风险与应对

| 风险 | 概率 | 影响 | 应对 |
|---|---|---|---|
| MADDPG 在事件流信用稀疏下不收敛 | 中 | 主表缺一行 | 如实报告(对标 QMIX 处理),分析纳入 §6 专题,本身构成 off-policy 适配性证据 |
| GA 收敛质量不达 0.95 分位线 | 低 | GA 定位弱化 | 升档 P×G(时延余量 10 倍)或如实报告 gap 与部署权衡 |
| 学习类 v4 重训超预算(EC-MAPPO 6h / 基线 3h) | 中 | 工期顺延 | 墙钟上限硬停兜底;EC-MAPPO 必要时降 iters 保联合样本量对齐(v4 说明 §10 原则);基线档超时如实记录 iters_done |
| **3h 档压缩导致基线性能低估(v1.1 新增)** | 高 | 基线数字被误读为算法上限 | 报告 §8 强制预算分层声明 + 警示语;MAPPO/QMIX/MADDPG 同档保证横向公平;EC-MAPPO 对基线的领先幅度须按"完整档 vs 3h 档"双口径讨论,不得单方面归因于算法优劣 |
| CPLEX 30 seeds 终评与既有 10 seeds 探针数字漂移 | 低 | 口径混乱 | 探针仅作验收参照,正式数字一律以 30 seeds 终评为准 |
| `--policy` 注册改动破坏 v3 回归 | 低 | 复现性断裂 | 改动后重跑 v3 关键回归抽查(hash 比对),v3/v4 双口径共存 |

---

## 11. 工期估算(CPU 串行,含 20% 缓冲)

| 阶段 | 估时 |
|---|---|
| S0 冒烟 + 定档 | 0.5 天 |
| S1 求解器 + S2 规则终评 | 1 天 |
| S3 学习类重训(基线 ×3 各 ≤3h + EC-MAPPO ≤6h) | 1–1.5 天 |
| S4 学习类终评 + 泛化 | 1 天 |
| S5 汇总报告 + V'' 验收 | 1 天 |
| **合计** | **≈ 4.5–5.5 天** |

