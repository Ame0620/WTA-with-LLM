# 对比实验说明：论文实验部分 9 算法复现手册（v3 数据集）

> **用途**：论文实验部分画图前的算法底稿——每个算法的机制、冻结参数（checkpoint）位置、训练/评估/汇总的完整复现命令。
> **范围**：仅本文档列出的 9 个算法（以《实验报告_v3论文书写.md》中选定行为准），不含其它算法。
> **数据集**：`data/dn-data-v3`（3 平台 × 50 威胁 × K=10，池 18 发；train s03–s26 / val s27–s30 / **test s01–s02**；test 总价值均值 3880.5；切分协议固定于 `data/dn-data-v3/MANIFEST.md`）。
> **核查日期**：2026-09-23（权重与产物均已在磁盘逐项核实）。

---

## 0. 九算法速览与权重状态

| # | 论文命名 | 类别 | 消融开关 (D,E,C) | 冻结权重位置 | 权重状态 |
|---|---|---|---|---|---|
| 1 | MAPPO⁰-r1 | 学习·消融骨架（r1 轮） | 000 | `output/e49_v3_ablation/b0_000_s{0,1,2}/best.pt` | ✅ 在（3 seeds） |
| 2 | Random | 规则 | — | 无（纯规则） | — |
| 3 | Greedy-Score | 规则 | — | 无（纯规则） | — |
| 4 | CPLEX | 优化·短视 | — | 无（逐拍求解） | — |
| 5 | QMIX | 学习·值分解 | — | `output/e25_qmix/best.pt` | ✅ 在（branch01 实测可评/可重训） |
| 6 | CASP-v1 | 学习·消融臂（r1 轮） | 001 | `output/e49_v3_ablation/only_001_s{0,1,2}/best.pt` | ✅ 在（3 seeds） |
| 7 | +EAPS-v2 | 学习·消融臂（r2 轮） | 010 | `output/e53_v3_ablation_r2/only_010_s{0,1,2}/best.pt` | ✅ 在（3 seeds） |
| 8 | +DCCA-v2 | 学习·消融臂（r2 轮） | 100 | `output/e53_v3_ablation_r2/b1_100_s{0,1,2}/best.pt` | ✅ 在（3 seeds） |
| 9 | EC-MAPPO (Ours) | 学习·CTDE | 111 | `output/e20_retrain/c010/best.pt` | ✅ 在（branch01 实测可评/可重训） |

消融开关位序 = (DCCA, EAPS, CASP)。r1 轮 = e49–e51（档 D 预算，系数 λ=α=β=1）；r2 轮 = e52–e55（档 A 预算，重标定 (λ,α,β)=(0.2, 0.5, 1.0)，锁定于 `logs/recal_selection.json`）。

---

## 1. 公共前置（所有算法通用）

```bash
cd /Users/fgod/Desktop/FGOD/Projects/UAS/WTA-Dn-branch01
PY=/opt/anaconda3/envs/wta/bin/python        # torch 2.13.0 / numpy 2.2.6
```

- **训练数据切分**（固定）：train = s03–s26（24 例），val = s27–s30（4 例），test = s01–s02（2 例）。
- **终评协议**：`experiments/dn_family_eval.py --split test`，30 个 MC 种子（`--seed-base 42`，即 42–71），默认带每拍集中式 CPLEX 参考解（`--no-ref` 关闭）。
- **统一评估器**：`experiments/dn_family_eval.py`（规则/优化/学习策略同一入口，产物 `family_report.json/md`，逐实例 `result_hash` 可复现校验）。
- **指标口径**：①泄漏率↓ ②命中/发射↑ ③无效交战率↓ ④期望摧毁价值↑ ⑤时延（主表 P50/P90 秒含参考解；消融臂为 µs 级纯前向，两套口径不可同列比较）。
- **统计协议**：规则/优化/QMIX/EC-MAPPO = 单训练种子 × 30 MC；消融臂 = 3 训练种子 × 30 MC × 2 test 实例 = **180 配对单元/臂**。

---

## 2. 逐算法复现流程

### 2.1 MAPPO⁰-r1（消融骨架，e49 c1，开关 000）

**机制**：统一消融训练器（`marl/train.py` 三开关全关）——StateCritic V(s) + PoolMLPNet 8 维 actor，无 DCCA 信用摊派、无 EAPS 势塑形、无 CASP 注意力头。参数量 10242。
**角色**：消融表 v1 轮基线（180 单元泄漏率 0.6384）。

**冻结参数**：`output/e49_v3_ablation/b0_000_s0/best.pt`、`..._s1/best.pt`、`..._s2/best.pt`（含 `ablation={use_dcca:0, use_eaps:0, use_casp:0}`、`budget`、`train_seed`、`best_val` 元数据）。

**训练**（档 D：500 iters × 64 eps，patience 20，eval-every 25，系数默认 λ=α=β=1；与 `scripts/run_e49_batch.sh` 等价的单臂命令）：

```bash
for s in 0 1 2; do
  $PY marl/train.py --data-dir data/dn-data-v3 --seed $s \
    --iters 500 --episodes-per-iter 64 --patience 20 --eval-every 25 \
    --use-dcca 0 --use-eaps 0 --use-casp 0 \
    --output output/e49_v3_ablation/b0_000_s$s
done
```

**评估**（30 MC × 2 test 实例，带 CPLEX 参考解缓存；等价 `scripts/run_e50_batch.sh` 单臂）：

```bash
for s in 0 1 2; do
  $PY experiments/e50_eval_entry.py --split test --policy ecmappo \
    --model output/e49_v3_ablation/b0_000_s$s/best.pt \
    --seeds 30 --seed-base 42 \
    --data-dir data/dn-data-v3 --manifest data/dn-data-v3/MANIFEST.md \
    --output output/e50_v3_ablation_b0_000_s${s}_eval
done
```

**汇总**：`$PY experiments/e51_summary.py` → `logs/ablation_v3_summary.json`（main 块 = 各臂 180 单元五指标 + h1–h8 检验）。

**画图取数**：`logs/ablation_v3_summary.json` → `arms["c1"]`；逐种子明细在 `output/e50_v3_ablation_b0_000_s{0,1,2}_eval/family_report.json`。

> ⚠️ 用**当前工作区代码**重新评估 r1 权重会被 `marl/policy.py` 的 r2 门禁拒绝（r1 检查点无 `reshaping` 字段，属刻意设计）。复评需回退到 r1 时点代码（`git worktree add ../r1snap 39e24a7`，S0 快照的 policy.py 无此门禁，可加载 e49 权重）。**若仅画图，直接用已冻结的 summary/family_report JSON，无需重跑。**

---

### 2.2 Random（规则，p_hold=0.3）

**机制**：合法动作集内按保持概率 0.3 随机发射（评估器内置，`--p-hold` 缺省即 0.3）。

**评估**（无训练）：

```bash
$PY experiments/dn_family_eval.py --split test --policy random \
  --seeds 30 --seed-base 42 --timelimit 30 \
  --data-dir data/dn-data-v3 --manifest data/dn-data-v3/MANIFEST.md \
  --output output/e22_baselines/random_ph030
```

**画图取数**：`output/e22_baselines/random_ph030/family_report.{json,md}`（泄漏率 0.8466±0.0130，时延 ≈0）。

---

### 2.3 Greedy-Score（规则）

**机制**：w·p_eff / windows_left 三要素加权评分贪心（本项目主启发式，评估器 `--policy greedy`）。

**评估**（无训练）：

```bash
$PY experiments/dn_family_eval.py --split test --policy greedy \
  --seeds 30 --seed-base 42 --timelimit 30 \
  --data-dir data/dn-data-v3 --manifest data/dn-data-v3/MANIFEST.md \
  --output output/regress_e12_greedy
```

**画图取数**：`output/regress_e12_greedy/family_report.{json,md}`（泄漏率 0.7913±0.0095）。

---

### 2.4 CPLEX（优化·短视最优）

**机制**：每拍集中式求解 WTA-MILP（短视 myopic，gap≡0 的来源；求解器前端 `cplex/wta_cplex.py`，社区版规模限制 1000 变量/约束内）。

**评估**（无训练）：

```bash
$PY experiments/dn_family_eval.py --split test --policy cplex \
  --seeds 30 --seed-base 42 --timelimit 30 \
  --data-dir data/dn-data-v3 --manifest data/dn-data-v3/MANIFEST.md \
  --output output/regress_e13_cplex
```

**画图取数**：`output/regress_e13_cplex/family_report.{json,md}`（泄漏率 0.7085±0.0343）。

---

### 2.5 QMIX（学习·值分解，e25）

**机制**：值分解 + 混合网络端到端训练（`marl/train_qmix.py`，agent 参数量 10242，eps-greedy 0.1→0.02 退火，DDQN 式双 Q，val 早停协议与 MAPPO 同）。

**冻结参数**：`output/e25_qmix/best.pt`（88821 字节，best_val 0.8089 @ iter 75、params 10242）——已核验可加载，且 **branch01 评估链路实测通过**（2026-09-23：单实例 × 2 seeds 冒烟，leak 0.8740，报告正常落盘）；`train_log.jsonl`（训练曲线数据）同目录在位。

**训练**（e25 时点参数 = 3000×128 / patience 60 / eval-every 25 / seed 0 / cpu；注意**当前代码默认值已改为 v5 档 1200×12**，必须显式传参）：

```bash
$PY marl/train_qmix.py --algo qmix --data-dir data/dn-data-v3 \
  --device cpu --seed 0 --iters 3000 --eval-every 25 --patience 60 \
  --episodes-per-iter 128 --output output/e25_qmix
```

**评估**：

```bash
$PY experiments/dn_family_eval.py --split test --policy qmix \
  --model output/e25_qmix/best.pt \
  --seeds 30 --seed-base 42 --timelimit 30 \
  --data-dir data/dn-data-v3 --manifest data/dn-data-v3/MANIFEST.md \
  --output output/e25_qmix_eval
```

**画图取数**：`output/e25_qmix_eval/family_report.md`（泄漏率 0.8262±0.0480）——报告已冻结；重评/补指标直接按上面命令执行即可（权重在位、评估链路已实测）。
**兼容性**：qmix 走独立加载路径，不受 r2 门禁影响，无需任何开关。

---

### 2.6 CASP-v1（消融臂 001，r1 轮）

**机制**：骨架 + 仅开 CASP（MarlNet 集合注意力 actor，10 维输入，含 CASP 头参数量 28482）。

**冻结参数**：`output/e49_v3_ablation/only_001_s{0,1,2}/best.pt`（`ablation={0,0,1}`）。

**训练**（同 §2.1 档 D，仅开关不同）：

```bash
for s in 0 1 2; do
  $PY marl/train.py --data-dir data/dn-data-v3 --seed $s \
    --iters 500 --episodes-per-iter 64 --patience 20 --eval-every 25 \
    --use-dcca 0 --use-eaps 0 --use-casp 1 \
    --output output/e49_v3_ablation/only_001_s$s
done
```

**评估/汇总/取数**：同 §2.1（model 路径换 `only_001_s$s`，输出目录 `output/e50_v3_ablation_only_001_s${s}_eval`；汇总走 `e51_summary.py`，JSON 中臂名 `c2`）。180 单元泄漏率 0.6224。
⚠️ 同样受 r1 门禁限制（见 §2.1 注意事项）。

---

### 2.7 +EAPS-v2（消融臂 010，r2 轮）

**机制**：骨架 + 仅开 EAPS 势塑形（R_shaped = R_team + λ·(γΦ′−Φ)），r2 重标定系数 λ=0.2。

**冻结参数**：`output/e53_v3_ablation_r2/only_010_s{0,1,2}/best.pt`（已核验元数据 `reshaping={phi_scale:0.2, credit_alpha:0.5, cf_beta:1.0}`、`ablation={0,1,0}`）。

**训练**（档 A：3000×128 / patience 60 / cpu；系数从锁定文件 `logs/recal_selection.json` 读入，与 `scripts/run_e53_batch.sh` 等价）：

```bash
for s in 0 1 2; do
  $PY marl/train.py --data-dir data/dn-data-v3 --seed $s \
    --iters 3000 --episodes-per-iter 128 --patience 60 --eval-every 25 \
    --device cpu \
    --use-dcca 0 --use-eaps 1 --use-casp 0 \
    --phi-scale 0.2 --credit-alpha 0.5 --cf-beta 1.0 \
    --output output/e53_v3_ablation_r2/only_010_s$s
done
```

**评估**（等价 `scripts/run_e54_batch.sh` Phase 1 单臂；须导出防串臂守卫）：

```bash
export MARL_EXPECT_RESHAPING="0.2,0.5,1.0"
for s in 0 1 2; do
  $PY experiments/e50_eval_entry.py --split test --policy ecmappo \
    --model output/e53_v3_ablation_r2/only_010_s$s/best.pt \
    --seeds 30 --seed-base 42 \
    --output output/e54_v3_ablation_only_010_s${s}_eval
done
```

**汇总**：`$PY experiments/e55_summary.py` → `logs/ablation_v3r2_summary.json`（含 mechanism 块的 r_shape_share 埋点：010 臂 0.0799）。

**画图取数**：`logs/ablation_v3r2_summary.json` 臂 `c3`（180 单元泄漏率 0.6109±0.0591）；机制证据（势塑形占比）也在该 JSON 的 `mechanism` 块。
**兼容性**：r2 检查点带 `reshaping` 字段，当前代码可直接评估 ✅。

---

### 2.8 +DCCA-v2（消融臂 100，r2 轮）

**机制**：骨架 + 仅开 DCCA（联合动作 critic + 击杀信用摊派 α=0.5 + 反事实 hold 差 β=1.0 注入个体优势）。

**冻结参数**：`output/e53_v3_ablation_r2/b1_100_s{0,1,2}/best.pt`（`ablation={1,0,0}`，`reshaping` 同上）。

**训练/评估/汇总**：同 §2.7，仅开关换 `--use-dcca 1 --use-eaps 0 --use-casp 0`，输出 `b1_100_s$s` / `output/e54_v3_ablation_b1_100_s${s}_eval`，JSON 臂名 `c5`。

**画图取数**：`logs/ablation_v3r2_summary.json` 臂 `c5`（180 单元泄漏率 0.6163±0.0493；credit_adv_share 埋点 0.1168）。

---

### 2.9 EC-MAPPO (Ours)（学习·CTDE）

**机制**：MAPPO 骨架 + DCCA/EAPS/CASP 三模块全开（λ=α=β=1，与 r2 轮 111 臂系数不同、训练器谱系不同——Ours 行勿与消融 111 臂混用）。参数量 28482。
**角色**：本文方法；主表口径 test 泄漏率 0.5963±0.0168（30 MC × 单训练种子）。

**冻结参数**：`output/e20_retrain/c010/best.pt`（145461 字节，best_val 0.6141880914842817 @ iter 250、params 28482，与 `train_summary.json` 逐位一致）——**branch01 评估链路实测通过**（2026-09-23：`MARL_ALLOW_LEGACY=1` 下单实例 × 2 seeds 冒烟，leak 0.6426，报告正常落盘）；`train_log.jsonl`（70 个评估点，画训练曲线可用）同目录在位。

**训练**（e20 干净重训 c010 臂；当前代码默认三开关全开 + 系数 1.0，与 e20 时点语义等价）：

```bash
$PY marl/train.py --device cpu --seed 0 --iters 3000 --eval-every 25 \
  --patience 60 --episodes-per-iter 128 --c-invalid 0.01 \
  --output output/e20_retrain/c010
```

**评估**（`--policy marl`，带参考解；原版权重为 v3 时点格式，需显式 opt-in 遗留通道）：

```bash
MARL_ALLOW_LEGACY=1 $PY experiments/dn_family_eval.py --split test --policy marl \
  --model output/e20_retrain/c010/best.pt \
  --seeds 30 --seed-base 42 --timelimit 30 \
  --data-dir data/dn-data-v3 --manifest data/dn-data-v3/MANIFEST.md \
  --output output/e20_eval/c010
```

**画图取数**：`output/e20_eval/c010/family_report.md`（五列全冻结：0.5963±0.0168 / 83.2% 命中列见主表 / 0.1095 / 1565.4 / P50 0.41、P90 0.45）——重评直接按上面命令执行即可。
**兼容性（2026-09-23 已解决）**：原版检查点为 e20 时点格式（无 `ablation/reshaping` 字段）。`marl/policy.py` 已内置显式遗留通道——评估前 `export MARL_ALLOW_LEGACY=1` 即按历史语义加载（set_attention + 全 10 维 x + 无重整形，等价于产出该权重的 S0 代码路径），加载时打印 WARNING 留痕；**不设该变量时门禁保持严格报错**（防误用不变）。若按上面命令重训，新检查点自带元数据、无需开关，但数值会偏离已冻结的论文数字（0.5963±0.0168）。

---

## 3. 冻结参数总表（MARL 检查点清单）

| 目录 | 内容 | 数量 | 状态 |
|---|---|---|---|
| `output/e49_v3_ablation/{8臂名}_s{0,1,2}/best.pt` | r1 轮全臂（本文用 b0_000、only_001） | 24 | ✅ 全在 |
| `output/e53_v3_ablation_r2/{8臂名}_s{0,1,2}/best.pt` | r2 轮全臂（本文用 only_010、b1_100） | 24 | ✅ 全在 |
| `output/e52_recal/*/best.pt` | e52 重标定网格（系数锁定依据） | 12 | ✅ 在 |
| `output/e20_retrain/c010/best.pt` | **EC-MAPPO (Ours) 主模型** | 1 | ✅ 在（branch01 可评/可重训） |
| `output/e25_qmix/best.pt` | **QMIX 基线模型** | 1 | ✅ 在（branch01 可评/可重训） |

每个 `best.pt` 内嵌元数据：`state_dict`、`feature_spec`、`ablation`（r1/r2 产物）、`reshaping`（仅 r2 产物）、`budget`、`train_seed`、`best_val`、`iter`、`versions`。

**权重就位状态（2026-09-23 核验并实测）**：上表两枚 `best.pt` 及配套 `train_log.jsonl` 均已在位、`torch.load` 核验元数据与训练摘要逐位吻合，且两条评估链路在 branch01 **实测跑通**。branch01 全库（数据/报告/日志）已确认无外部路径残留。
**branch01 实测记录（2026-09-23，冒烟后产物已清理）**：
1. `MARL_ALLOW_LEGACY=1` + `--policy marl`（e20 原版权重）：加载 WARNING 留痕、params 28482 对齐、单实例 × 2 seeds 评估 leak 0.6426、报告正常落盘 ✅
2. `--policy qmix`（e25 原版权重，无需开关）：单实例 × 2 seeds 评估 leak 0.8740、报告正常落盘 ✅
3. `marl/train.py` 重训冒烟（25 iters / 30s）：新检查点自带 `ablation/reshaping` 元数据、params 28482 与原版一致、**默认严格门禁下直接可加载** ✅
后续注意：①画图本就无需权重（数字已冻结在各 report/summary）；②EC-MAPPO 原版权重评估须带 `MARL_ALLOW_LEGACY=1`（见 §2.9/§4.1）；③QMIX 无任何附加要求。

---

## 4. 风险与红线（复现/画图前自查）

1. **r1 与 e20 原版权重复评门禁（2026-09-23 起有内置通道）**：`marl/policy.py` 默认仍强制检查点含 `ablation`+`reshaping` 字段（r2 spec §3.2/§3.8）——r1（e49）与 e20 原版权重不设开关会被拒（防误用语义不变）。复评这两类权重时评估命令前加 `MARL_ALLOW_LEGACY=1`，即按历史语义加载并打印 WARNING 留痕；QMIX 不受影响（独立加载路径）；仅画图则无此问题。
2. **r2 复评须导出守卫**：`export MARL_EXPECT_RESHAPING="0.2,0.5,1.0"`（防串臂；值源 `logs/recal_selection.json`）。
3. **跨块禁比**：本文档混用了 r1 臂（#1 MAPPO⁰-r1、#6 CASP-v1，档 D 500×64）与 r2 臂（#7 EAPS-v2、#8 DCCA-v2，档 A 3000×128）——两轮预算与系数不同，泄漏率绝对值互不可直比；与主表口径（#2–#5、#9，档 A 单训练种子 × 30MC）也不可直比。画图时注明各臂所属轮次/口径。
4. **QMIX 重训参数陷阱**：当前 `marl/train_qmix.py` 默认值是 v5 档（1200×12 / patience 12），复现 e25 必须显式传 §2.5 的 v3 档参数。
5. **CPLEX 参考解缓存**：消融评估经 `experiments/e50_eval_entry.py` 走 `output/_cplex_ref_cache/`（110 个 .sol），删缓存会显著拉长重评时间但不改数值。
6. **MAPPO⁰ ≠ 标准 MAPPO**：#1 是统一消融训练器骨架（10242 参数），与主表中未列入本文档的标准 MAPPO 基线是两条不同实现，数字不得互换。

---

## 5. 画图取数路由（一页速查）

| 算法 | 数据文件 |
|---|---|
| MAPPO⁰-r1 | `logs/ablation_v3_summary.json`（臂 c1）；明细 `output/e50_v3_ablation_b0_000_s*_eval/family_report.json` |
| Random | `output/e22_baselines/random_ph030/family_report.json` |
| Greedy-Score | `output/regress_e12_greedy/family_report.json` |
| CPLEX | `output/regress_e13_cplex/family_report.json` |
| QMIX | `output/e25_qmix_eval/family_report.md`（无 .json 存档，数值以 .md 为准） |
| CASP-v1 | `logs/ablation_v3_summary.json`（臂 c2）；明细 `output/e50_v3_ablation_only_001_s*_eval/` |
| +EAPS-v2 | `logs/ablation_v3r2_summary.json`（臂 c3 + mechanism.r_shape_share）；明细 `output/e54_v3_ablation_only_010_s*_eval/` |
| +DCCA-v2 | `logs/ablation_v3r2_summary.json`（臂 c5 + mechanism.credit_adv_share）；明细 `output/e54_v3_ablation_b1_100_s*_eval/` |
| EC-MAPPO (Ours) | `output/e20_eval/c010/family_report.md/.json` |

> 所有 `family_report` 均含逐实例 `result_hash`，可与《实验报告_v3论文书写.md》表中数字逐位核对。

---

*本手册基于 2026-09-23 磁盘实物核查（权重元数据逐一加载验证）。命令均自 `scripts/run_e4{9,50}_batch.sh`、`scripts/run_e5{2,3,4}*.sh`、`experiments/e50_eval_entry.py` 与《实验报告_MARL核心框架.md》§9.1 命令块还原。*
