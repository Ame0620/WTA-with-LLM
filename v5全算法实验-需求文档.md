# DN-WTA v5 全算法实验 — 需求文档

> **文档版本**:v1.1(2026-09-18)
> **v1.1 变更**:新增 §6.1 **全流程自动化与守护要求**——S0–S5 一键编排、执行代理全程待命、watchdog 周期巡检运行与输出、训练断点续跑、崩溃自愈;合法停止(早停/跑满/墙钟到限)后自动衔接下游环节;同步涉及 §3.2、§8(V13''')、§9、§10、§11
> **预算口径(v1.0 核心)**:**EC-MAPPO(本文方法)放宽至全流程 ≤12h**(训练墙钟 ≤10.5h + 评估 ≤1.5h)以达成其可达最佳性能;**其余 8 算法全流程 ≤3h**(学习类基线训练墙钟 ≤2.5h)
> **上游依据**:`DN-WTA_v5_数据集说明.md`(v5 基准)、`v4全算法实验-需求文档.md`(算法族与协议基线)、`实验报告_v4全算法总览.md`(v4 数字来源)
> **交付目标**:在 v5 数据族(10 平台 × 100 目标 × 全局池 70)上完成 **9 算法** 全量终评,并产出《实验报告_v5全算法总览.md》
> **本文档性质**:需求设计文档(不含算法实现),作为后续开发、验收与报告撰写的唯一依据

---

## 0. 默认决策声明(待确认项的缺省取值)

以下决策点已按合理缺省值锁定,如需变更请在开发启动前提出,否则按本节执行:

| # | 决策点 | 缺省取值 | 理由 |
|---|---|---|---|
| D1 | 算法范围 | **全量 9 算法**(v4 终评 9 算法全部平移 v5 终评,**无新增算法**) | v4 已完成 MADDPG/GA 引入,本期焦点是资源扩展验证,非算法扩充 |
| D2 | **预算分层(本期核心)** | **EC-MAPPO 完整档:全流程 ≤12h;其余 8 算法一律 3h 全流程档**(学习类训练墙钟 ≤2.5h) | 用户明确要求:本文方法放宽预算追最佳性能;基线为"资源受限参照",3h 档由墙钟硬保证,报告须显式声明口径差异 |
| D3 | EC-MAPPO 采样档 | 缺省 **2500 iters × 96 ep/iter + patience 60**(v4 完整档采样量平移);S0 smoke 实测速率后,若预算余量充足可上调(§3.3) | v5 单步开销 ≈ ×2 vs v4,预估训练 6–7h,12h 预算下保留上调空间以达最佳性能 |
| D4 | 学习基线采样档 | 缺省 **1200 iters × 12 ep/iter**(≈ v4 基线档采样量一半,匹配 v5 ×2 单步开销),MAPPO/QMIX/MADDPG **同档同墙钟** | v4 基线档 1200×24 在 v5 下墙钟将超限;按开销比例压缩采样量,三基线公平性红线 |
| D5 | 报告结构 | 沿用 v4 总览结构,**v4↔v5 跨资源并表**替换 v3↔v4 跨规模并表;MADDPG vs MAPPO、GA vs CPLEX 专题保留并聚焦 m=10 表现 | v5 定位即"资源扩展验证基准",跨资源对比为必答题 |
| D6 | 可选扩展(本期不做) | 多训练种子方差估计、EC 模块跨算法族迁移、资源弹性(m 插值)扫描 | 列入 §12 后续方向,主实验聚焦 |

---

## 1. 背景与目标

v5 是 v4 的**资源侧各向同性扩展**:m 5→10、全局池 30→70(mu 6→7)、池/需求 0.67→**0.78**、理论拦截上限 30%→**70%**;目标流(k_arr/r0/w/total_value)同 seed 逐值继承(v5 说明 §10),协议(30 MC seeds / split 划分 / seed 隔离)与指标体系完全一致。问题瓶颈从"弹药稀缺下的目标取舍"转向"**协同质量与火力分配效率**"(单步火力密度首次达到到达密度)。

实验目标:

- **G1 主表**:9 算法在 v5 test(s01–s02 × 30 seeds)统一终评,产出与 v4 §5 同口径主表;
- **G2 跨资源对比**:9 算法 v4↔v5 归一化指标并表(仅归一化,v5 说明 §8),量化资源扩展(平台 ×2 + 弹药 ×2.33)对各算法族的增益/衰减;
- **G3 机制验证**:①弹药压力放宽(0.78)下"过度节约弹药"次优模式检验(前瞻留弹边际价值预期低于 v4);②MADDPG(off-policy)与 GA(元启发式)在 m=10 下的定位复验;
- **G4 可复现性**:全部结果 result_hash 逐位复现,资源不变量(`shots ≤ 70`)零违例。

---

## 2. 算法总矩阵(9 算法)

| # | 算法 | 类别 | v4 状态 | v5 工作量 | 对比角色 |
|---|---|---|---|---|---|
| 1 | Random(p_hold=0.3) | 规则 | 已有 | **零代码**(切数据路径) | 随机下界 |
| 2 | Greedy-Nearest | 规则 | 已有 | **零代码** | 局部贪心参照 |
| 3 | Greedy-Threat | 规则 | 已有 | **零代码** | 局部贪心参照 |
| 4 | CPLEX | 优化(短视精确) | 已有 | **零代码**;若探针 `e22_v5_cplex_probe` 未跑先补跑;30 seeds 正式终评 | 短视最优参照(gap=0 源) |
| 5 | GA(短视) | 优化(元启发式) | 已有 | 染色体长度确认由 m 推导(若硬编码 5 需改)+ m=10 下收敛/时延复验 | 低成本逼近短视最优 |
| 6 | QMIX | 学习·值分解 | 已有 | v5 重训(**3h 档**)+ critic 维度适配 | 值分解失效假设的资源侧验证 |
| 7 | MAPPO | 学习·CTDE(on-policy) | 已有 | v5 重训(**3h 档**)+ critic 维度适配 | 标准 CTDE 消融参照 |
| 8 | MADDPG | 学习·CTDE(off-policy) | 已有 | v5 重训(**3h 档**)+ critic 维度适配 | off-policy 确定性策略对照 |
| 9 | EC-MAPPO(本文方法) | 学习·CTDE(on-policy+定制) | 已有 | v5 重训(**完整档 ≤12h**,§3.3)+ critic 维度适配 | 本文方法在资源扩展下的最佳性能 |

---

## 3. v5 适配需求详述(全算法通用)

### 3.1 维度适配点(代码级,预计全部为适配级小改)

| 文件 | 适配点 | v4 → v5 | 说明 |
|---|---|---|---|
| `marl/baseline_net.py` | `N_AGENTS` 常量 | 5 → 10,**推荐改为由实例 m 推导** | 影响 QMIX mixer / MAPPO critic 输入维度;推导化后后续版本零改动 |
| `marl/train.py` | CriticNet 联合动作维度 3·m | act [B,15] → **[B,30]** | v5 说明 §11 明示的训练侧适配点 |
| `marl/maddpg_net.py` | CentralQCritic 输入 | 35 + m×8:75 → **115** | v4 规格已要求"维度由 m 推导、不得硬编码",**代码审查确认**;若已推导则零改动 |
| `dwta/ga_solver.py` | 染色体长度 | 5 → 由实例 m 推导 | v4 规格为"长度 m 整数染色体",确认无硬编码 |
| actor 侧(全部算法) | **零改动** | — | MarlNet / PoolMLPNet 均为置换不变集合网络,对 m/n 天然泛化 |
| `dwta/dn_env.py` / `dn_instance.py` | **零修改** | — | v5 说明 V1'' 已验证(2101 行,30/30 解析通过) |

**适配验收(进入训练前置)**:m=10 下参数量仍在 [1e3, 1e5](复用 `assert_params`);critic/mixer 输入维度打印核对;3 实例 × 5 seeds 冒烟零非法动作。

### 3.2 训练预算分层(v1.0 核心口径)

**分层原则(D2)**:EC-MAPPO(本文方法)放宽预算以验证资源扩展下可达最佳性能;三个学习基线压缩至 3h 全流程档,且三者保持**同 iters×episodes 采样档 + 同墙钟上限**(差异只剩训练范式本身)。

| 算法 | 全流程预算 | 训练墙钟上限 | iters × ep/iter(采样档) | 早停 patience | 说明 |
|---|---|---|---|---|---|
| **EC-MAPPO** | **≤12h(完整档)** | 10.5h | 2500 × 96(S0 smoke 后可上调,§3.3) | 60 eval 点 | v4 完整档采样量平移;12h 预算下保留上调空间 |
| MAPPO | **≤3h(基线档)** | 2.5h | 1200 × 12(=14,400 ep) | 12 eval 点 | 与 QMIX/MADDPG 同采样档同墙钟 |
| QMIX | **≤3h(基线档)** | 2.5h | 1200 × 12 | 12 eval 点 | 同上 |
| MADDPG | **≤3h(基线档)** | 2.5h | 1200 × 12 | 6 eval 点 | off-policy 样本效率在固定墙钟下体现 |

配套缺省(训练器 CLI 缺省值与本表一致,§9 复现命令):

- **评估耗时核算**:学习类终评 30 seeds(挂 per-step CPLEX 参考)+ 泛化三 split(`--no-ref`)——v5 下 CPLEX 参考解耗时 ≈ ×2–3 vs v4,基线档合计仍按 ≤0.5h 预留(与 v4 同量级偏保守);**EC-MAPPO 档评估预留 1.5h**(12h − 10.5h);
- **求解器/规则类**:CPLEX 30 seeds 终评预估 1–2h(m=10 求解变慢,以探针实测定档)< 3h;GA 主档预估数分钟;规则类秒级——**均无预算风险,协议不缩水(30 seeds 全量)**;
- 墙钟到限即停(训练器内建 WALL_LIMIT_SEC),任何"iters 未跑满即停"如实记入 train_summary.json 并在报告 §8 声明;**到限/早停/跑满均属合法停止,结束后由编排器自动衔接该算法终评与泛化,不允许停在原地等待人工触发(§6.1)**。

### 3.3 EC-MAPPO 12h 档执行细则(本文方法最佳性能保障)

1. **缺省档**:2500 iters × 96 ep/iter(=240,000 ep,联合样本量与 v4 完整档持平)+ patience 60 eval 点;
2. **smoke 定档**(S0,0.5h):实测每 iter 速率 →
   - 若预估训练墙钟 < 8h:**上调采样档**(优先 `2500 × 128` 或 `3000 × 96`,保证联合样本量 ≥ v4 档且墙钟 ≤10.5h);
   - 若预估 > 10.5h:下调 iters 保墙钟,联合样本量尽量不低于 v4 档的 80%;
3. **早停不缩水**:patience 60 不变;val 评估点频率因评估开销 ×2 可放宽,但**不得低于每 25 iters 一次**;
4. **预算目标**:本文方法在 v5 上达到其可达最佳性能,12h 为上限而非目标(不刻意耗满);超参(lr/γ/batch)沿用 v4 已定值,仅采样档随预算调整,避免引入额外差异轴。

### 3.4 学习基线 3h 档压缩原则

- 三基线(MAPPO/QMIX/MADDPG)**同采样档 + 同墙钟上限**,公平性红线(报告 §6 须声明);
- 压缩方式:优先降采样量(iters×ep,1200×24 → 1200×12)而非降网络容量/批大小;**超参沿用 v4 已定值、不重新调参**(调参消耗预算且引入差异轴);
- 若 3h 档内不收敛:如实报告并分析(失败本身构成"资源受限下该范式适配性"证据,对标 v4 中 QMIX 的处理方式,不强行调参刷结果);
- 报告口径红线:基线 3h 档数字为**资源受限参照,不代表各范式收敛上限**,不得单方面与 EC-MAPPO 12h 档数字直接归因算法优劣(见 §7.8 双口径讨论要求)。

---

## 4. 既有 9 算法 v5 平移需求

### 4.1 零代码类(规则 + CPLEX)

```bash
--data-dir data/dn-data-v5 --manifest data/dn-data-v5/MANIFEST.md
```

- CPLEX:若探针 `e22_v5_cplex_probe`(test × 10 seeds,V7'')未执行则先补跑(兼作耗时定档);正式口径 **30 seeds(42–71)** 终评;
- 规则类:Random / Greedy-Nearest / Greedy-Threat 各 30 seeds 终评,参数沿用 v4(`--policy-seed` 等)。

### 4.2 GA(m=10 复验)

- 染色体长度 10(确认由 m 推导);主档 **P=40, G=50** 不变,敏感性三档 {20×25, 40×50, 80×100} 复跑;
- 复验 B 系列:收敛质量(GA/CPLEX 当拍价值比 ≥ 0.95 @95% 分位)、时延 P50 < 0.5s / P90 < 1s(适应度查表 O(m) 仍快,但搜索空间 ×2,需实测)、P×G 敏感性漂移 ≤ 2pp。

### 4.3 学习类(QMIX / MAPPO / MADDPG / EC-MAPPO)

- §3.1 维度适配 → §3.1 适配验收(冒烟)→ §3.2 预算分层重训;
- 训练曲线、checkpoint、train_summary.json 产出要求与 v4 完全一致。

---

## 5. 统一评估协议(v5)

与 v3/v4 完全一致(v5 说明 §8),锁定执行口径:

1. **正式对比**:test split s01–s02 × **30 MC seeds(42–71)**,per-instance mean±std + family 聚合;
2. **指标**:16 项主指标 + ⑰无效交战率 + ⑱弹药轨迹;主表呈现①泄漏率、④命中率、⑰无效率、期望摧毁价值、⑦时延 P50/P90、gap;**跨 v4/v5 对比仅用归一化指标**(total_value 同 seed 相同使归一无歧义);
3. **参考解**:greedy 族 / 学习族 / GA 均挂 per-step CPLEX 参考(gap 口径);泛化快照用 `--no-ref`;
4. **泛化**:学习算法另报 train/val/test 三 split(30 seeds,`--no-ref`),偏差 |D| 警戒线 0.10;
5. **资源不变量**:每 run 校验 `shots_total ≤ 70`、`ammo_end ≥ 0`(冒烟自检,全部算法纳入);
6. **复现**:family_report.json 记录 argv + source_md5;本期修改文件(§3.1)须更新 md5 清单。

---

## 6. 实验执行计划(顺序与依赖)

| 阶段 | 内容 | 依赖 | 产出 |
|---|---|---|---|
| S0 冒烟 + 定档 | 学习类 4 算法各 0.5h smoke(3 实例 × 5 seeds + 训练速率定档);CPLEX 探针补跑(若 V7'' 未执行,兼作终评耗时定档);GA m=10 时延实测 | 无 | 速率报告、预算定档(§3.3/§3.4) |
| S1 求解器终评 | CPLEX 30 seeds(42–71)+ GA 主档 + GA 敏感性三档 | GA 过 B 系列复验 | `output/e4x_v5_{cplex,ga*}` |
| S2 规则终评 | Random / Greedy×2(30 seeds) | 无 | `output/e4x_v5_baselines` |
| S3 学习重训 | MAPPO / QMIX / MADDPG(各 **≤3h 全流程档**,可串行)→ EC-MAPPO(**完整档 ≤12h**,建议夜间/独立机位) | S0 定档 | 各 `best.pt` + 训练曲线 |
| S4 学习终评 | 4 学习算法 test 30 seeds(含参考解)+ 三 split 泛化 | S3 | `output/e4x_v5_{ecmappo,mappo,qmix,maddpg}[_gen]` |
| S5 汇总报告 | 主表 + 跨资源并表 + 专题分析 + 回归核查 | S1–S4 | 《实验报告_v5全算法总览.md》 |

### 6.1 全流程自动化与守护要求(无人值守红线,v1.1 新增)

**总原则**:S0–S5 全流程**一键编排、无人值守执行**;任何环节完成后自动进入下一环节,**执行代理(编排器/watchdog)全程待命,禁止"跑完一步停在原地等待人工触发"**。具体要求:

1. **一键编排**:交付 `experiments/run_v5_pipeline.py`(或等价脚本),内置 §6 依赖图,单命令驱动 S0→S5;支持 `--stage` 从任一阶段续跑(幂等:已完成任务按产物存在性自动跳过);
2. **全程待命与周期巡检**:watchdog 常驻,缺省每 **5 min** 巡检一次,检查项——①子进程存活;②日志尾部增长(`logs/e4x_*.log` mtime);③关键产物更新(checkpoint / train_summary.json / family_report.json);**连续 3 个周期无任何输出增长 → 判定挂起**,留存现场(日志尾部 + 进程栈)后自动处置;
3. **断点续跑**:4 个训练器周期性落盘 checkpoint(每 ≤10 min 或每 eval 点,含 iter 计数与优化器状态)并支持 `--resume`;watchdog 重启训练后从最近 checkpoint 恢复,**墙钟预算扣除已运行时长**(总预算不变,不因重启放大);
4. **崩溃自愈**:任务异常退出(非合法停止)自动重启,单任务重启上限 **3 次**;仍失败则标记 failed 写入 `logs/pipeline_status.json`,**跳过该任务并继续执行不依赖它的后续任务**(依赖任务级联跳过,S5 汇总如实呈现)——不允许单点失败阻塞全流程;
5. **合法停止定义(唯一豁免清单)**:仅①早停触发、②iters 跑满、③墙钟到限(WALL_LIMIT_SEC)三类为合法停止,且合法停止后**同样由编排器自动衔接下游**(如训练结束 → 自动触发该算法终评 + 泛化);此外任何进程退出/挂起均视为异常,由 watchdog 捕获处置;
6. **可观测性**:`logs/heartbeat.json` 每周期刷新(各任务状态 / 最近输出时间戳 / 已重启次数 / 预算余量),供随时抽查进度;阶段切换与异常处置均追加写入 `logs/pipeline_status.json`。

---

## 7. 《实验报告_v5全算法总览.md》章节需求

结构沿用 v4 总览,跨规模并表升级为跨资源并表:

1. 执行摘要:9 算法层次表 + 三条核心结论,须覆盖"v4 层次是否在资源扩展下保持";
2. 实验基准:DN-WTA v5(资源扩展参数对照表、池/需求 0.78、拦截上限 70%、结构性下界 ≈14% 价值);
3. 评估协议与指标:同 v4 口径声明 + 归一化并表规则;
4. 对比算法:9 个(机制/超参见 v4 报告,此处仅列 v5 适配点与差异声明);EC-MAPPO 定制模块照抄 v4 并声明零改动;
5. test 终评主表(9 行,列同 v4 §5)+ 结果分析;
   - **5.5 v4↔v5 跨资源并表(核心新增)**:9 算法 × {v4 泄漏率, v5 泄漏率, Δpp, 增益比},命中率/无效率同表;逐算法给"资源扩展敏感度"判词(如"规则类增益有限 / 学习类增益显著 / GA 稳定");
   - 分析必答:①层次保持性;②瓶颈迁移(弹药稀缺 → 协同质量)对各家族的位移方向;③拦截上限 30%→70% 对结论的影响;
6. MADDPG vs MAPPO 专题:样本效率曲线(墙钟–性能)、最终性能、稳定性;**两算法同为 3h 档(同采样档同墙钟)公平性声明**;聚焦 m=10 下信用摊派面 ×2 的表现差异;
7. GA vs CPLEX 专题:m=10 下 gap 分布(P50/P90)、时延对比、P×G 敏感性、部署价值结论;
8. 训练侧与泛化:4 学习算法参数量/训练步数/墙钟 + 三 split |D|;**须单列预算分层声明表(v1.0):EC-MAPPO 完整档 ≤12h vs 三基线 3h 档(1200×12 + 墙钟 2.5h),并显式警示"基线 3h 档数字为资源受限参照,不代表各范式收敛上限;EC-MAPPO 对基线的领先幅度须按'完整档 vs 3h 档'双口径讨论"**;
9. 支撑实验:资源不变量声明(`shots ≤ 70`)、v4 数字引用来源标注;
10. 关键发现汇总(编号列表;必含:弹药压力 0.78 下"过度节约弹药"次优模式是否出现——对照 v5 说明 §9 预判);
11. 复现命令与产物索引:算法名 ↔ `output/` 目录映射表;
12. 后续方向:含 D6 项。

**报告数字红线**:所有主表数字可溯源至 family_report.json;QMIX/MAPPO/EC-MAPPO/MADDPG/GA 的 v4 数字直接引用 v4 报告并标注出处;守恒校验(泄漏价值 + 摧毁价值 ≈ total_value,量级 8732)逐算法过一遍。

---

## 8. 总验收标准(V''' 系列)

S5 完成后统一执行,全项 PASS 方可定稿报告:

| # | 验收项 | 方法 | 通过标准 |
|---|---|---|---|
| V1''' | 协议合规 | 抽查各算法 family_report.json params | test 2 实例 × seeds=30、seed_base=42、v5 data-dir |
| V2''' | 资源不变量 | 全部 9 算法 × 全部 runs | `shots ≤ 70`、`ammo_end ≥ 0`,零违例 |
| V3''' | 零非法动作 | run 记录 illegal_actions 求和 | 全部为 0 |
| V4''' | 结果可复现 | 任选 3 算法重跑 5 seeds 比对 | result_hash 逐位一致 |
| V5''' | 维度适配正确性 | m=10 下参数量 + critic/mixer 输入维度自动推导 | 学习类 4 算法参数量 ∈ [1e3, 1e5];无硬编码维度告警 |
| V6''' | GA 收敛与时延 | m=10 下 B1–B4 复验 | 全过(≥0.95 分位、P50<0.5s/P90<1s、敏感性 ≤2pp) |
| V7''' | 时延红线 | 全算法 latency_p90 | < 10s 决策周期(学习类目标 < 1s) |
| V8''' | 训练预算(分层) | 学习类 4 算法墙钟(训练 + 评估) | **EC-MAPPO ≤ 12h;MAPPO/QMIX/MADDPG 各 ≤ 3h** |
| V9''' | 泛化偏差 | 学习类三 split | \|D\| < 0.10(警戒线) |
| V10''' | 跨资源口径 | 报告 §5.5 检查 | 仅归一化指标并表;v4 数字与 v4 报告一致 |
| V11''' | 守恒校验 | 泄漏价值 + 摧毁价值 vs total_value | 逐算法 9/9 通过 |
| V12''' | 回归不破坏 | v4 关键抽查(3 算法 × 5 seeds 重跑) | result_hash 与 v4 报告产物逐位一致 |
| V13''' | 无人值守全流程 | 审查 heartbeat/pipeline_status 全程日志;人为 kill 一个训练进程验证自愈 | 自动恢复后同 seed result_hash 与不中断运行逐位一致;S0–S5 全程零人工触发、零未处置异常退出 |

---

## 9. 交付物与目录规划

**代码(预计全部为适配级小改,无新算法)**:

- `marl/baseline_net.py` — `N_AGENTS` 改由实例 m 推导(或改 10);
- `marl/train.py` — CriticNet 联合动作维度 3·m(act [B,30]);
- `marl/maddpg_net.py` — 审查 CentralQCritic 维度推导(35 + m×8 = 115),如已推导则零改动;
- `dwta/ga_solver.py` — 审查染色体长度 m 推导,如硬编码需改;
- `experiments/dn_family_eval.py` — source_md5 清单更新;
- `experiments/run_v5_pipeline.py` — S0–S5 一键编排 + watchdog 周期巡检 + 崩溃自愈 + `logs/heartbeat.json`(§6.1);
- `marl/train.py`、`marl/train_{mappo,qmix,maddpg}.py` — 补周期 checkpoint 落盘(≤10 min)+ `--resume` 断点续跑 + 心跳可观测输出;
- `tests/test_v5_algorithms.py`(建议)— V5'''/V2''' 冒烟 + 资源不变量自动检查。

**实验产物(output/,目录名即报告 §11 索引)**:

| 目录 | 内容 |
|---|---|
| `output/e40_v5_cplex/` | CPLEX 30 seeds 正式终评 |
| `output/e41_v5_ga/{p40g50,p20g25,p80g100}/` | GA 主档 + 敏感性 |
| `output/e42_v5_baselines/{random,greedy_nearest,greedy_threat}/` | 规则类 |
| `output/e43_v5_{ecmappo,mappo,qmix,maddpg}/` | 学习类训练(checkpoint/曲线) |
| `output/e44_v5_{...}_eval/`、`e45_v5_{...}_gen/` | 学习类终评与泛化 |
| `logs/e4x_*.log` | 各阶段运行日志 |

**报告**:项目根目录《实验报告_v5全算法总览.md》(§7 结构)。

**复现命令模板**(报告 §11 用,口径对齐 v4):

```bash
PY=/opt/anaconda3/envs/wta/bin/python
V5="--data-dir data/dn-data-v5 --manifest data/dn-data-v5/MANIFEST.md"

# 求解器与规则类
$PY experiments/dn_family_eval.py --split test --policy cplex --seeds 30 \
    $V5 --output output/e40_v5_cplex
$PY experiments/dn_family_eval.py --split test --policy ga --ga-pop 40 --ga-gen 50 \
    --seeds 30 $V5 --output output/e41_v5_ga/p40g50

# 学习类基线(3h 档:训练器缺省 = 1200 iters × 12 ep + 墙钟 2.5h,无需显式传参)
$PY marl/train_mappo.py  --data-dir data/dn-data-v5 --output output/e43_v5_mappo
$PY marl/train_qmix.py   --algo qmix --data-dir data/dn-data-v5 --output output/e43_v5_qmix
$PY marl/train_maddpg.py --data-dir data/dn-data-v5 --output output/e43_v5_maddpg
# EC-MAPPO(完整档:2500 iters × 96 ep + patience 60,墙钟 10.5h;S0 后若上调按定档值改写)
$PY marl/train.py --data-dir data/dn-data-v5 --iters 2500 \
    --episodes-per-iter 96 --output output/e43_v5_ecmappo

# 终评 → 泛化(四算法同构;以 ecmappo 为例)
$PY experiments/dn_family_eval.py --split test --policy ecmappo \
    --model output/e43_v5_ecmappo/best.pt --seeds 30 $V5 --output output/e44_v5_ecmappo_eval
$PY experiments/dn_family_eval.py --split train --policy ecmappo --no-ref \
    --model output/e43_v5_ecmappo/best.pt --seeds 30 $V5 --output output/e45_v5_ecmappo_gen
```

---

## 10. 风险与应对

| 风险 | 概率 | 影响 | 应对 |
|---|---|---|---|
| CPLEX 30 seeds 终评超 3h(m=10 求解变慢) | 中 | 工期/预算超限 | S0 探针实测先行;超限时微调 per-step 求解 gap 容差控制在 3h 内并如实报告,协议(seeds)不缩水 |
| **3h 档压缩导致基线性能低估(口径风险)** | 高 | 基线数字被误读为算法上限 | 报告 §8 强制预算分层声明 + 警示语;三基线同档保证横向公平;EC-MAPPO 领先幅度按"12h 完整档 vs 3h 档"双口径讨论 |
| 学习类 v5 重训超预算 | 中 | 工期顺延 | 墙钟上限硬停兜底;EC-MAPPO 按 §3.3 定档流程预留余量;基线档超时如实记录 iters_done |
| EC-MAPPO 12h 档在 m=10 下收敛慢/不稳定 | 中 | 本文方法数字不佳 | patience 60 + 训练曲线全量呈现;S0 smoke 定档上调采样档;不通过削减评估点数挤占训练预算 |
| MADDPG 联合动作嵌入维度硬编码 75(未按 m 推导) | 低 | 运行时崩溃 | V5''' 代码审查前置;若发现按 §3.1 修复并重跑冒烟 |
| 维度适配改动破坏 v3/v4 回归 | 低 | 复现性断裂 | V12''' 强制 v4 关键回归抽查(hash 比对),三版本口径共存 |
| 弹药压力 0.78 下"过度节约弹药"新模式干扰结论解读 | 低 | 分析误导 | 报告 §10 必答项:对照弹药轨迹指标 ⑱ 检验,与 v5 说明 §9 预判交叉验证 |
| 长时任务(EC-MAPPO ≤12h)夜间挂起/崩溃无人发现 | 中 | 预算浪费、工期顺延 | §6.1 watchdog 全程待命:周期巡检 + 自动重启(≤3 次)+ 心跳日志;断点续跑不重计预算 |

---

## 11. 工期估算(CPU 串行,含 20% 缓冲)

| 阶段 | 估时 |
|---|---|
| S0 冒烟 + 定档(含 CPLEX 探针补跑)+ 流水线/守护脚本搭建 | 0.5–1 天 |
| S1 求解器 + S2 规则终评 | 1 天 |
| S3 学习类重训(基线 ×3 各 ≤3h 串行 + EC-MAPPO ≤12h 夜间/独立机位) | 1.5–2 天 |
| S4 学习类终评 + 泛化 | 1 天 |
| S5 汇总报告 + V''' 验收 | 1 天 |
| **合计** | **≈ 5–6 天**(EC-MAPPO 与基线并行排程、全流程无人值守运行时) |



