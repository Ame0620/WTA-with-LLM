# 需求文档：DN-WTA v3 EC-MAPPO 三模块消融·第二轮（e52 重标定 / e53 训练 / e54 评估 / e55 汇总）

> **轮次定性**：上轮消融（e49–e51，《实验报告_v3消融.md》）已完成八臂全因子 × 3 训练种子，验收主条款（c8 vs c1 差 ≥ 0.01 且显著）达成，但存在用户明确不满意的两点结果（见 §1）。本轮为**根因修复后的全量重跑**，含三轮次：受控管线改造（三个缺省位级等价的重标定参数）→ 预注册调参轮（e52，仅 train/val）→ 正式消融轮（e53–e55，档 A 预算 × 24 臂）。
> **上游规范**：《EC-MAPPO三模块界定.md》（模块语义与红线，简称「界定文档」）；《需求文档_v3消融实验.md》（上轮需求，简称「上轮文档」）；《DN-WTA_v3_数据集说明.md》与 `data/dn-data-v3/MANIFEST.md`（数据口径）。
> **执行环境**：macOS / zsh；解释器 `/opt/anaconda3/envs/wta/bin/python`（简写 `$PY`）；项目根 `/Users/fgod/Desktop/FGOD/Projects/UAS/WTA-Dn-branch01`；训练设备缺省 `auto`。
> **编号占用现状**：output 已用至 e50 系、experiments 已用至 e51。本轮固定 **e52（重标定调参）/ e53（正式训练）/ e54（终评+泛化）/ e55（汇总）**，不得复用 e49–e51 任何目录名。

---

## 0. 问题诊断（基于 e49–e51 实证，本轮全部对策的依据）

### 0.1 两点不满意（用户意见）

| # | 现象（上轮实测，test 泄漏率，180 runs/臂） | 用户要求 |
|---|---|---|
| **P1** | c1 (0,0,0) = 0.6384、c8 (1,1,1) = 0.6237，总提升仅 −0.0147 | 扩大模块整体提升空间 |
| **P2** | c5 仅 DCCA = 0.7386（+0.1002 显著恶化）、c3 仅 EAPS = 0.6931（+0.0547 显著恶化） | 三个单开臂（c2/c3/c5）vs c1 全部要有正提升 |

附带结构问题 **P3**：c8 (0.6237) 劣于 c6 (0.6150)，即 EAPS 在最优组合上仍是拖累（c8↔c6 = +0.0086，p=3.7e-04）——P2 与 P3 同源。

### 0.2 三条根因（代码级 + 实证级证据）

**R-A｜预算不足压缩模块增益空间（P1 主因）**
- 上轮锁档 D（500 iters × 64 eps，patience 20），而 e20 主表 EC-MAPPO 为档 A（3000×128，patience 60）1750 iters 早停达 0.5963；上轮 c8 仅 0.6237——模块增益被预算截断约 0.027；
- 24 臂中 3 例未收敛全部集中在 c5（2/3）与 c3（1/3），均为学习最慢臂；
- v5 家族对照：档 A 下仅 DCCA（c5 0.4257）与基线持平无害，档 D 下 c5 全场最差——**DCCA 边际随预算增大单调改善**，小预算下其反事实 Critic 与信用信号来不及被消化。

**R-B｜EAPS 塑形项尺度失配（P2/P3 中 EAPS 部分的结构性根因）**
- 代码现状（`marl/reward.py`）：`R_shaped = R_team + phi_sign·(GAMMA_SHAPE·Φ_{t+1} − Φ_t)`，塑形项以 **1:1 满幅**叠加，尺度系数隐含为 1，无任何可调参数；`GAMMA_SHAPE=0.99` 为硬编码常量；
- 势差项逐步非零（密度高）而基础事件奖励稀疏，满幅叠加使训练信号被中间塑形主导，v3/v5 双族方向一致负贡献（v5 的 c3/c4 同样显著差于同族）——**结构性失配而非随机波动**。

**R-C｜DCCA 个体信用三通道 1:1:1 满幅注入（P2 中 DCCA 部分的结构性根因）**
- 代码现状（`marl/train.py` `process_batch`）：`adv_i = GAE_team + credit + cf_row[i]`，回溯信用与反事实差权重硬编码为 1；
- credit 单笔量级 ≈ w_j/total（击杀价值归一），通常远大于逐步 GAE 量级；batch 内优势标准化 `(adv−mean)/std` 只吸收绝对量级，**三通道相对比例与均值偏移完全由权重决定**——满幅注入使个体优势被信用项主导，与 c5 的病态行为吻合（重复瞄准 3.03 全场最高、弹药效率 56.2 全场最低：信用信号驱使火力反复砸向高价值目标）。

### 0.3 对策映射（C1–C4）

| # | 对策 | 针对根因 | 预期效应（量化依据） |
|---|---|---|---|
| **C1** | 正式轮预算升级：档 D → **档 A**（3000×128，patience 60，与 e20 主表同款） | R-A | c8 回到 0.5963 邻域（e20 实证）；c5/c3 获得足够预算消化模块信号 |
| **C2** | 新增 `--phi-scale λ` 并重标定：`R_shaped = R_team + λ·phi_sign·(γΦ_{t+1}−Φ_t)` | R-B | c3 单开转正；c8 卸下 EAPS 拖累（上轮 c8−c6 = +0.0086 → 预期 ≤ 0） |
| **C3** | 新增 `--credit-alpha α`、`--cf-beta β` 并重标定：`adv_i = GAE + α·credit + β·cf` | R-C | c5 单开转正（抑制信用主导，缓解重复瞄准病态） |
| **C4** | CASP 不动 | — | c2 单开已是唯一独立有益单模块（−0.0160，p=8.3e-08），任何改动只有下行风险 |

叠加预期：c8−c1 由 −0.0147 扩大到 **−0.025 ~ −0.045** 量级（c1 档 A 预计 0.61–0.64，c8 修复后预计 0.58–0.61）；此为预期区间（sanity 用），非验收承诺。

### 0.4 方法学合规声明（重标定的合法性）

界定文档 §7.1：「不得单独为某一消融臂调参。若确需调参，应预先规定所有实验臂共同使用的搜索空间和选择规则。」本轮据此执行：

1. **网格预注册**：§4 的 λ 与 (α,β) 网格在训练开始前冻结，中途不得增删（扩展预案 P1/P2 亦预先注册）；
2. **选择规则预注册**：只凭 **val split** 泄漏率选择（test 全程隔离，R5/R10）；
3. **全臂统一**：选出的 (λ*, α*, β*) 同时应用于 e53 全部 24 臂（含 c1——但三参数仅在对应模块开启时进入计算图，c1 数值上不受影响，语义上仍是 MAPPO⁰，见 §3 E2 验收）；
4. **调参轮与正式轮隔离**：e52 全部产物独立目录，其 val 数字不进入正式报告主表，仅作选择链证据。

---

## 1. 用户硬性要求与验收分层

### 1.1 要求①：扩大提升空间（针对 P1）

- **目标 T1**：c8 vs c1 的 test 泄漏率差 ≤ **−0.030**（上轮 −0.0147 的 2 倍），180 配对单元双侧 Wilcoxon p < 0.05，双指标（泄漏率/期望摧毁）3/3 种子方向一致；
- **底线 B1**：差 ≤ −0.020 且统计显著（较上轮至少扩大 36%）；
- **配套 T1′**：c8 为八臂最优，或与最优臂差异不显著（配对 Wilcoxon p ≥ 0.05）。

### 1.2 要求②：单模块全部正提升（针对 P2）

- **目标 T2**：c2、c3、c5 vs c1 三组配对比较的泄漏率差点估计全部 < 0（改善），且三组 Wilcoxon p < 0.05；
- **底线 B2**：三组点估计均 ≤ 0，且无一组显著恶化（p < 0.05 且方向为恶化）。

### 1.3 诚实条款与达成机制

实验结果无法在文档层面「保证」；本轮通过 C1–C3 最大化达成概率，并设置未达预案（§10）：B1/B2 未达时，允许按预注册预案追加一轮调参（仍仅 val），两轮后仍未达则如实报告并给出机制归因。**禁止任何作弊性达成手段**（R11）：不得对 c1 或任何臂单独降预算/改超参、不得用 test 反馈调参、不得篡改 MAPPO⁰ 定义。

---

## 2. 数据与资产冻结

| 项 | 冻结值 |
|---|---|
| 数据目录 | `data/dn-data-v3/`（仅此一目录）；命令一律显式 `--data-dir data/dn-data-v3 --manifest data/dn-data-v3/MANIFEST.md` |
| 划分 | test = s01–s02 / train = s03–s26 / val = s27–s30（同上轮，MANIFEST 双通道解析一致） |
| test 参考锚 | s01/s02 总价值 3942/3819；none 泄漏率 1.0000（指纹 s01 `9134a16f` / s02 `e717a4c1`） |
| MC 评估种子 | `--seeds 30 --seed-base 42`（与上轮/e20 口径一致） |
| 管线改动范围 | 仅 §3 列出的三个参数 + 机制埋点 + 审计增补，其余零改动；改造前基线 commit 记录进报告 |
| 上轮产物引用 | `logs/ablation_v3_summary.json`（两轮对比表数据源）、`output/_cplex_ref_cache/`（e54 直接复用，参考解与 policy 无关）、`output/_pre_ablation_ref/`（等价性验收参照） |
| 硬不变量 | 每回合 shots ≤ 18、ammo_end ≥ 0、零非法动作、零 NaN；Actor/Critic 参数量 ∈ [1e3, 1e5] |

配置臂名与论文口径 c1–c8 映射**完全承上轮**（b0_000 / only_001 / only_010 / ad_011 / b1_100 / ae_101 / b2_110 / b3_111），不重复列表。

---

## 3. 管线改造（受控、可审计、缺省位级等价）

### 3.1 三个重标定参数

| 新 CLI 参数 | 缺省 | 生效条件 | 语义 |
|---|---|---|---|
| `--phi-scale`（float） | 1.0 | 仅 `--use-eaps 1` | `R_shaped = R_team + λ·phi_sign·(GAMMA_SHAPE·Φ_{t+1} − Φ_t)`；λ=1.0 与改造前逐位一致；`use_eaps=0` 时 λ 不进计算图（Φ 构建整体跳过语义不变） |
| `--credit-alpha`（float） | 1.0 | 仅 `--use-dcca 1` | 事件回溯信用注入权重：`adv_i = GAE + α·credit.get((t,i),0) + …`；`use_dcca=0` 时无 credit 注入路径 |
| `--cf-beta`（float） | 1.0 | 仅 `--use-dcca 1` | 反事实差权重：`… + β·(V(s,a) − V(s,a_{−i},hold_i))`；`use_dcca=0` 时不构建 hold 变体批（语义不变） |

实现红线：三参数为**纯乘性系数**，不得引入其他行为变化；`phi_sign` 继续冻结 −1.0（承上轮 R7——本轮只调幅度，不动符号）；`credit-mode`、`c-invalid`、`GAMMA_SHAPE` 均不动。

### 3.2 元数据与埋点

- checkpoint（best.pt/last.pt）新增 `"reshaping": {"phi_scale": .., "credit_alpha": .., "cf_beta": ..}`，与 `ablation` 三开关并列；评估器加载时**必须校验**该字段与 e52 选出值一致（不一致 raise，防串臂）；
- `_mech_row()` 新增两个机制字段（重标定生效证据，报告 §3 用）：
  - `r_shape_share`：窗口内 `mean|λ·φ_diff| / (mean|λ·φ_diff| + mean|R_team|)`（EAPS 臂的塑形占比）；
  - `credit_adv_share`：窗口内 `mean|α·credit + β·cf| / (… + mean|GAE|)`（DCCA 臂的个体信号占比）；
- 训练日志首行扩为 `[ablation] use_dcca=.. use_eaps=.. use_casp=..` + `[reshaping] phi_scale=.. credit_alpha=.. cf_beta=..`。

### 3.3 等价性与审计验收（改造合门槛禁，全部通过才准进入 e52）

- **E1 全开位级等价**：三参数全缺省的 (1,1,1) vs **改造前代码**（git stash 或基线副本），同 seed 0、`--iters 50 --eval-every 25`、train 实例：train_log 剔除 elapsed/wall 类字段后逐位一致，best.pt 张量逐位相等（口径同上轮 S2.2）；
- **E2 全关隔离**：(0,0,0) 任意 λ/α/β 取值（抽 {0.2, 2.0} 极端值）下训练 25 iters，与缺省值 (0,0,0) 逐位一致——证明三参数不进 MAPPO⁰ 计算图；
- **E3 静态审计增补**：`tests/test_ablation_switches.py` 由 35 项增至 ≥ 41 项，新增至少覆盖：λ 仅出现在 use_eaps 分支、α/β 仅出现在 use_dcca 分支、缺省值常量锁定、元数据 reshaping 字段存在性、评估器校验路径可达；
- 产物写入 `output/_pre_ablation_ref/r2_recheck_report.md`（含 E1/E2 命令回显与判定）。

---

## 4. S4 重标定调参轮（e52，预注册）

### 4.1 预算档 T（调参专用，冒烟定档）

`--iters 800 --episodes-per-iter 128 --patience 30 --eval-every 25`，单训练种子 0，仅 train/val split。定档依据：档 T 样本量 = 800×128 ≈ 档 D 的 3.2 倍，足以让上轮学习最慢的 c3（450 iter 收敛）/c5 显出 val 分化；冒烟实测 (1,0,0)/(0,1,0) 各 30 iters 记录 s/iter，单臂外推墙钟 ∈ [0.4h, 1.2h]，超界按 ±1 档调整 iters（640/1000）并记录于 `logs/recal_budget.json`。

### 4.2 预注册网格与臂构成

| 臂 | (D,E,C) | 参数 | 数量 | 用途 |
|---|---|---|---|---|
| R-base | (0,0,0) | 缺省 | 1 | 基线参照（三参数不进计算图） |
| E-λ | (0,1,0) | λ ∈ {0.2, 0.5} | 2 | EAPS 尺度网格（λ=1.0 即缺省行为，由 E-ref 补齐） |
| E-ref | (0,1,0) | λ=1.0（缺省） | 1 | 缺省对照（分离「本轮重跑噪声」与「λ 效应」） |
| D-αβ | (1,0,0) | (α,β) ∈ {(0.5,1), (1,0.5), (0.5,0.5)} | 3 | DCCA 权重网格 |
| D-ref | (1,0,0) | (1,1)（缺省） | 1 | 缺省对照 |
| F-full | (1,1,1) | 缺省 | 1 | 全开参照（保险丝基准） |
| F-pick | (1,1,1) | (λ*, α*, β*) | 1 | 保险丝：选出组合在全开下的 sanity |

共 10 臂。输出 `output/e52_recal/<臂名>/`，日志 `logs/e52_<臂名>.log`。

### 4.3 选择规则（预注册，执行顺序固定）

1. **EAPS**：候选集 = {λ ∈ {0.2, 0.5, 1.0}：E-λ/E-ref 臂的 val 最优泄漏率 ≤ R-base val 最优泄漏率 + 0.005}（0.005 为容差，量级参照上轮 val 跨种子 std ≈ 0.005–0.016）；候选非空 → 取 val 最优者 λ*（平手取更接近 1.0 者，最小改动原则）；候选空 → 启动预案 **P1**（扩展 λ=0.1 重跑 E-λ 一臂后重选；仍空 → EAPS 判定「本网格内尺度不可修复」，上报用户后再定，不擅自扩大搜索）。
2. **DCCA**：候选集 = {(α,β) ∈ 四网格点：D-αβ/D-ref 的 val 最优泄漏率 ≤ R-base + 0.005}；取 val 最优者 (α*, β*)（平手取更接近 (1,1) 者）；候选空 → 预案 **P2**（扩展 (0.25, 0.5) 与 (0.5, 0.25) 两点重跑后重选；仍空 → 上报，附机制归因）。
3. **保险丝**：F-pick 的 val 最优泄漏率 ≤ F-full 的 val 最优泄漏率 + 0.005 → 通过，锁定 (λ*, α*, β*)；不通过 → 取各模块次优组合重跑 F-pick 一次；仍不通过 → 预案 **P3**（按「单模块优先于全开」原则锁定单模块侧最优值，全开损失如实报告）。
4. 选择链全程写入 `logs/recal_selection.json`（每臂 val 曲线、候选集、取舍理由），**一旦 e53 开跑，参数不得再改**。

### 4.4 e52 验收

- 10 臂零 NaN/非法动作、元数据自洽（reshaping 字段与臂参数一致）；
- E1/E2/E3（§3.3）已通过；
- `recal_selection.json` 含完整选择链与预注册网格对照（无中途增删网格点的证据）。

---

## 5. S5 正式消融轮（e53，24 臂档 A）

- **预算档 A**（首选，= e20 主表同款）：`--iters 3000 --episodes-per-iter 128 --patience 60 --eval-every 25`；
- **合规窗口与阶梯**：S6 冒烟（(1,1,1) 与 (0,0,0) 各 50 iters 实测 s/iter）外推 24 臂总墙钟 ∈ [20h, 60h]；超 60h → 降 A−1（2000×128，patience 50），再超 → A−2（1500×128，patience 40）；**降档全臂同步**并写入 `logs/ablation_v3r2_budget.json` 与报告 §1（口径：与 e20 档 A 不可直接对比的声明）；
- **三参数**：全部 24 臂统一传 e52 选出值（λ*/α*/β*），显式写入命令（不留缺省），八臂命令除三开关与三参数外逐字节一致；
- **执行序**（承上轮）：s0 八臂 → s0 轻量检查点（逐臂自检 + c8↔c1 方向 sanity + **c3/c5 vs c1 方向 sanity**，异常即停止上报）→ s1 八臂 → s2 八臂；臂序同上轮（c1 → c8 → c4 → c6 → c7 → c5 → c3 → c2）；
- 单臂命令模板（`<s>` ∈ {0,1,2}）：

```bash
$PY marl/train.py --data-dir data/dn-data-v3 --seed <s> \
  --iters 3000 --episodes-per-iter 128 --patience 60 --eval-every 25 \
  --use-dcca <d> --use-eaps <e> --use-casp <c> \
  --phi-scale <λ*> --credit-alpha <α*> --cf-beta <β*> \
  --output output/e53_v3_ablation_r2/<臂名>_s<s> > logs/e53_<臂名>_s<s>.log 2>&1
```

- 冻结缺省（承上轮 R7）：`--credit-mode=credit_kill`、`--phi-sign=-1.0`、`--c-invalid=None`、`--anneal-iters=2000`、批采样缺省开、`--device=auto`；`--init-from/--init-blend/--resume/--resume-from` 全程禁止；
- 逐臂自检（承上轮 S4）：首行双开关行与臂配置一致、checkpoint 元数据（ablation + reshaping）双字段与臂一致、零 NaN/非法、shots ≤ 18、参数量窗口、墙钟；训练曲线里程碑仍记「首个 val < 0.75 的 iter」+ best_val@iter。

---

## 6. S6 终评 + 泛化（e54）

协议**全文承上轮 S5**（`scripts/run_e54_batch.sh` 镜像 `run_e50_batch.sh`，幂等），差异点：

1. 路径/编号替换：`e49_v3_ablation → e53_v3_ablation_r2`、`e50_* → e54_*`；
2. test × 30 seeds × 2 实例 + train/val/test 泛化（`--no-ref`）逐臂执行，24 臂全覆盖；
3. **CPLEX 参考解直接复用** `output/_cplex_ref_cache/`（参考解只依赖 (实例, MC seed)，与 policy/参数无关）——缓存命中即跳过求解，未命中才补算并回写缓存；
4. 可复现自检：c8 s0 test 终评全量复跑一次，result_hash 逐位一致（剔 wall_time 类字段）；
5. 评估器加载 checkpoint 时校验 reshaping 字段 = (λ*, α*, β*)（§3.2，冲突 raise 运行时可达）。

---

## 7. S7 汇总（e55）与报告

新增 `experiments/e55_summary.py`（适配自 `e51_summary.py`：路径 → e53/e54 系、输出 `logs/ablation_v3r2_summary.json`、`budget_tier` 承 `logs/ablation_v3r2_budget.json`、新增 `reshaping` 字段透传）。产出承上轮 S6 全部 7 项（主表/机制表/配对比较/八配置描述/泛化/跨种子稳定性/跨家族对照），另新增：

1. **两轮对比表（本轮核心新增）**：c1–c8 × {上轮（档 D，缺省参数）泄漏率 | 本轮（档 A，重标定）泄漏率 | Δ}，配对单元口径同（180/臂）；档位与参数差异随表申明；
2. **重标定证据表**：λ/α/β 网格各点 val 最优泄漏率、候选集与选择链（引自 `recal_selection.json`）、r_shape_share / credit_adv_share 前后对比（重标定生效的机制证据）；
3. **验收核对表**：§1 的 T1/B1/T1′/T2/B2 逐条判定（含未达条款的预案执行记录）；
4. 配对比较四组核心对承上轮（c8↔c1、c8↔c4、c8↔c6、c8↔c7），另增**单开三对**（c2↔c1、c3↔c1、c5↔c1）为要求②的正式判据（180 配对单元 Wilcoxon + 3 种子方向票）。

报告《实验报告_v3消融_第二轮.md》骨架承上轮（§1 预算与参数声明 → §2 主表+两轮对比 → §3 机制表 → §4 配对比较 → §5 结论 → §6 附录：网格选择链、跨种子稳定性、R1–R11 自查、交付物索引）。

---

## 8. 总预算核算

| 阶段 | 规模 | 估算 |
|---|---|---|
| S1–S3 门禁+改造验收 | E1/E2 双跑 + 审计 | ≈ 1h |
| S4 e52 调参轮 | 10 臂 × 档 T（0.4–1.2h/臂，冒烟定档） | 4–12h |
| S5 e53 正式轮 | 24 臂 × 档 A（早停依赖，参照 e20 1750 iters 早停 0.65h，上界 3000 iters） | 24–48h（窗口 [20h, 60h]，超限降 A−1） |
| S6 e54 终评 | 24 终评 + 72 泛化，CPLEX 缓存复用后 | ≈ 0.25h |
| S7 e55 汇总 | 脚本一次 | < 0.1h |
| **合计** | | **≈ 29–62h**，跨夜批（`run_e53_batch.sh` 幂等支持中断续跑，夜间编排器可复用 `orchestrator_night.sh` 模式） |

---

## 9. 红线 R1–R11

R1–R9 全文承上轮（历史 MAPPO 隔离 / 统一宿主 / 从头训练 / 同档同超参 / test 隔离 / 数据纯净 / 冻结缺省 / 档位申报 / 维度推导），本轮新增与修订：

| # | 红线 | 本轮锚点 |
|---|---|---|
| **R4′** | 同档同超参（修订） | 24 臂命令除三开关（+语义上无效的 c1 侧三参数）外逐字节一致；λ*/α*/β* 全臂统一传值 |
| **R5′** | test 隔离（强化） | e52 调参轮**只允许 train/val**；test 仅 e54 触碰；调参选择链不得引用任何 test 数字 |
| **R10** | 调参预注册 | 网格、容差、选择规则、预案 P1–P3 以本文档为准冻结；e53 开跑后参数不得再改；`recal_selection.json` 须能证明无中途增删 |
| **R11** | 禁止作弊性达成 | 不得对 c1（或任何单臂）单独降预算/改超参/削弱；不得以 test 反馈迭代参数；不得修改 MAPPO⁰ 定义（E2 验收锁定 c1 计算图不变）；不得在报告中隐去未达条款 |

---

## 10. 验收清单与未达预案（S8）

| 验收条款 | 判据 |
|---|---|
| 管线改造等价性 | E1 逐位一致 / E2 逐位一致 / E3 审计 ≥ 41 项通过 |
| e52 调参轮 | 10 臂健康跑完、选择链完整、参数锁定并申报 |
| e53 正式轮 | 24 臂零 NaN/非法、元数据双字段自洽、预算合规 |
| e54/e54 产物 | 24 终评 + 72 泛化、c8 复现抽查 hash 一致 |
| **要求①（T1/B1）** | c8↔c1 泄漏率差：目标 ≤ −0.030，底线 ≤ −0.020 且 Wilcoxon 显著 + 3/3 种子同向 |
| **要求②（T2/B2）** | c2/c3/c5↔c1 三对：目标全部显著改善；底线全部点估计 ≤ 0 且无显著恶化 |
| T1′ | c8 为最优臂或与最优差异不显著 |
| 报告完整性 | 两轮对比表、重标定证据表、验收核对表、R1–R11 自查表齐备 |

**未达预案**（按序执行，全程只用 val，结果如实入报告）：
- B2 中 c3 或 c5 仍显著恶化 → 该模块启动 P1/P2 二次扩展网格（各 +1 轮，最多一次）；
- B1 未达但 T2 达成 → 报告如实呈现「单模块修复成功、组合增益受限」，归因交互效应（c8↔c6/c4 配对数据）；
- 二次调参后仍未达底线 → 终止迭代，出归因报告（区分「预算上限」「网格覆盖不足」「模块本身不适配本任务」三种解释的证据边界），交用户决策后续方向。

**交付物**：`scripts/run_e52_recal.sh`、`run_e53_batch.sh`、`run_e54_batch.sh`；`experiments/e55_summary.py`；`output/e52_recal/`（10 臂）、`output/e53_v3_ablation_r2/`（24 臂）、`output/e54_*`（24 终评 + 72 泛化 + 复现抽查）；`logs/recal_budget.json`、`recal_selection.json`、`ablation_v3r2_budget.json`、`ablation_v3r2_summary.json`；《实验报告_v3消融_第二轮.md》。

执行顺序：S1 门禁 → S2/S3 改造与等价验收 → S4 e52 → 参数锁定 → S5 e53 → S6 e54 → S7 e55 → S8 报告；E1–E3 任一失败即停止，不得带病进入 e52。

---

## 11. 非目标

- 不修改 MAPPO⁰ 定义、不动 CASP（网络/输入/记忆）、不动 `phi_sign`/`credit-mode`/`c-invalid`/`GAMMA_SHAPE`；
- 不做参数量匹配对照臂、跨数据集混合训练、v4/v5 复跑；
- 不改主表（e20）历史数字，不重评上轮 e49 checkpoint（reshaping 字段缺失会被 e54 评估器拒绝加载，属预期行为，不修）；
- 预算敏感性专项（档 C 中间档复跑 c5/c6/c8）不列入本轮，作为后续可选项。
