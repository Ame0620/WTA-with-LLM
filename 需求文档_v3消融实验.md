# 需求文档：DN-WTA v3 EC-MAPPO 三模块消融（e49 训练 / e50 评估 / e51 汇总）

> **轮次定性**：v5 消融轮（e46/e47/e48，《实验报告_v5消融.md》）已用统一三开关宿主完成八臂全因子 × 1 训练种子。本轮在**论文主表数据集 v3（3×50，池 18）**上复用同一协议并**升级为 3 训练种子（seed 0/1/2，8 配置 × 3 种子 = 24 臂，一次交付正式结论）**，属于**纯执行轮**：三开关管线已建成并通过 B.3/B.4 验收，**预期零管线代码改动**，仅新增批处理脚本、汇总脚本与本轮产物。
> **上游规范**：《EC-MAPPO三模块界定.md》（模块语义与红线，本文简称「界定文档」）；《需求文档_v5消融实验.md》（管线建设需求，已完成）；《DN-WTA_v3_数据集说明.md》与 `data/dn-data-v3/MANIFEST.md`（数据口径）。
> **执行环境**：macOS / zsh；解释器一律 `/opt/anaconda3/envs/wta/bin/python`（下文简写 `$PY`）；工作目录 `/Users/fgod/Desktop/FGOD/Projects/UAS/WTA-Dn-branch01`（下文简写「项目根」）；训练设备缺省 `auto`（v3 历史轮实际落 cpu，0.65h/臂量级，无需 GPU）。
> **编号占用现状**：output 已用至 e47（e48 仅 `experiments/e48_summary.py` 汇总脚本，无输出目录）。本轮**固定使用 e49（训练）/ e50（终评+泛化）/ e51（汇总脚本与产物）**，不得复用 e46–e48 任何目录名。

---

## 消融配置定义（论文口径 c1–c8）

三个消融维度即 EC-MAPPO 的三个创新模块（记号承界定文档）：

- **DCCA** — 判别式批评家：联合状态-动作批评家 `CriticNet(s,a)` + 击杀信用注入的个体优势；
- **EAPS** — 势差动作塑形：事件优势势函数 Φ 的塑形奖励 `R_team + phi_sign·(γΦ_{t+1}−Φ_t)`；
- **CASP** — 上下文感知集合感知 Actor：集合注意力网络 `MarlNet` + λ̂/n̂ 记忆输入（x∈R¹⁰）。

八配置 = 三维度全因子，位序 (D,E,C) 按二进制自 c1 递增至 c8：

| Ablation config. | DCCA (critic & credit) | EAPS (reward shaping) | CASP (actor & memory) | 对应臂名（目录/日志用） |
|---|:---:|:---:|:---:|---|
| c1 (MAPPO⁰) | ✗ | ✗ | ✗ | `b0_000` |
| c2 (+CASP) | ✗ | ✗ | ✓ | `only_001` |
| c3 (+EAPS) | ✗ | ✓ | ✗ | `only_010` |
| c4 (EAPS+CASP) | ✗ | ✓ | ✓ | `ad_011` |
| c5 (+DCCA) | ✓ | ✗ | ✗ | `b1_100` |
| c6 (DCCA+CASP) | ✓ | ✗ | ✓ | `ae_101` |
| c7 (DCCA+EAPS) | ✓ | ✓ | ✗ | `b2_110` |
| **c8 (EC-MAPPO full)** | ✓ | ✓ | ✓ | `b3_111` |

- **c1 = 基础 MAPPO**（MAPPO⁰，独立规范，见 §0.2）；**c8 = EC-MAPPO 全量**（论文框架，语义保证见 §0.1）；c2–c7 为中间配置。
- **递增路径**：c1→c5→c7→c8（+DCCA→+EAPS→+CASP）；**核心留一配对**：整体 c8↔c1、DCCA 条件贡献 c8↔c4、EAPS 条件贡献 c8↔c6、CASP 条件贡献 c8↔c7。
- 目录、脚本、日志沿用臂名（与 v5 轮惯例及幂等跳过逻辑兼容）；**论文/报告/汇总表格一律用 c1–c8 编号**（可括注臂名溯源）。

---

## 0. 目标与主问题

在 v3 数据集上，以统一宿主 `marl/train.py` 的三个构造期开关（`--use-dcca/--use-eaps/--use-casp`）执行八臂全因子消融 × 3 训练种子（seed 0/1/2，共 24 臂），回答界定文档 §1 的五问：

1. **整体有效性**：EC-MAPPO 全量 c8 (1,1,1) 相对基础 MAPPO c1 (0,0,0) 在主指标（test 泄漏价值比例①）上是否改善？
2. **单模块条件贡献**：DCCA / EAPS / CASP 各自的「从全量中去掉」的边际效应（c8↔c4 / c8↔c6 / c8↔c7 留一配对）？
3. **是否存在普适贡献模块**：跨单元格主效应方向是否一致？
4. **模块组合是否存在交互**（描述性为主，辅以跨种子方向一致性，不作正式检验）：重点复核 v5 轮发现的 D×E 交互（「EAPS 开而 DCCA 关」明显不利、DCCA 开时被抵消）是否在 v3 家族复现；
5. **工程成本**：参数量 / 纯策略前向时延的开关差（v3 上复测）。

**与 v5 轮的关系**：v3 是论文主表（实验报告_v3全算法总览）所用数据集；本轮结论与 v5 轮结论构成**跨家族（3×50 vs 10×100）稳健性互证**，但两轮预算档位不同，交叉对照仅作描述性讨论（见 §7.3 红线 R8）。

### 0.1 用户硬性要求①：全量实现的语义保证

(1,1,1) 臂必须等价于 v3 主表中 EC-MAPPO（test 泄漏率 0.5963±0.0168，e20_retrain/c010）所用的**同一算法框架全量**，证据链如下（本轮逐条落实）：

- **代码级**：`marl/train.py` 三开关全开 = 改造前 EC-MAPPO 训练路径，已由 v5 轮 B.3 验收**位级证实**（`output/_pre_ablation_ref/B3_equivalence_report.md`：eval 点逐位一致、best/last.pt 位级一致、batch probe 360 条张量统计逐位相等、3-seed 终评 result_hash 逐位一致）；该证明与数据集无关，v3 直接引用；
- **超参级**：(1,1,1) 采用与 e20 同款预算档（§4 档 A：3000×128×patience 60）与同款模块缺省（credit_kill、Φ 符号 −1.0、c_invalid 缺省=0.01 与 e20 显式 `--c-invalid 0.01` 等值）；
- **本轮复验**：S2 的确定性双跑 + 静态审计 35/35 + checkpoint 元数据断言（§5.4）。

**口径申明（不得省略）**：e20_retrain 系 2026-09 修复后重训产物，README 标注其上游含 resume-from 谱系，且其 RNG 播种上下文与当前宿主（Trainer 构造期显式 `torch.manual_seed`）不同；因此 b3_111 各种子（s0/s1/s2）均为**从头训练**，**不要求复现 0.5963**，预期落在同档同分布邻域。报告中主表数字仅作参考区间引用，禁止宣称「复现了主表数值」。

### 0.2 用户硬性要求②：MAPPO⁰ 的纯净性

(0,0,0) 臂必须是**规范的基础 MAPPO**，按界定文档 §0 的 MAPPO⁰ 定义独立成立，**不得参考任何 EC-MAPPO 专属内容**：

- Actor：`PoolMLPNet`（均值池化 + MLP），输入 x∈R⁸（`drop_m1=True` 剔除 M1 记忆列 λ̂/n̂，AgentMemory 递推保留但不进网络）；
- Critic：`StateCritic` V(s)（动作块不构建），共享团队 GAE（个体优势 = 团队优势）；
- 奖励：`R_shaped = R_team.clone()`（Φ 构建整体跳过）；
- 恒定公共底座：MAPPO 核心更新（clip PPO、GAE、批采样）、公共动作接口、可行性掩码、基础事件奖励。

**R1 隔离红线**：全程不得引用历史 MAPPO 训练器（`marl/train_mappo.py`，本仓库当前不存在）及其任何产物（v4/v5 轮的 `e33_v4_mappo`、`e43/e44/e45_v5_mappo*` 等）作底座、参照、表格或解释；(0,0,0) 的达标判据是上述计算图性质 + 训练健康度（S2c），**而非**与任何历史 MAPPO 数值的比对。

---

## 1. 数据与资产冻结

| 项 | 冻结值 |
|---|---|
| 数据目录 | `data/dn-data-v3/`（仅此一目录，R6） |
| MANIFEST | `data/dn-data-v3/MANIFEST.md`（训练/评估命令一律显式传 `--data-dir data/dn-data-v3 --manifest data/dn-data-v3/MANIFEST.md`） |
| 家族参数 | m=3（平台）、n=50（目标）、K=10（同时接战）、μ=6（全局弹药池 18） |
| 划分 | test=s01–s02（2 实例）/ train=s03–s26（24）/ val=s27–s30（4）；由 `marl/data_split.discover_split` 与评估器 MANIFEST 解析双通道给出，二者必须一致 |
| test 参考锚 | s01/s02 总价值 3942/3819（均值 3880.5）；下界 none 泄漏率 1.0000（e11 指纹 s01 `9134a16f` / s02 `e717a4c1`） |
| MC 评估种子 | 30 seeds，`--seeds 30 --seed-base 42`（缺省即此，显式传出） |
| 管线代码 | `marl/train.py`、`marl/reward.py`、`marl/policy.py`、`marl/perceive.py`、`marl/network.py`、`marl/baseline_net.py`、`experiments/dn_family_eval.py`、`dwta/dn_policies.py`（ecmappo 注册名）、`tests/test_ablation_switches.py`（35 项静态审计）——**S1 核查后冻结，除新增文件外零改动** |
| 已有验收资产 | `output/_pre_ablation_ref/B3_equivalence_report.md`、`B4_acceptance_report.md`、`switch_audit.json`（引用，不重跑） |
| 硬不变量 | 每回合 shots_total ≤ 18、ammo_end ≥ 0、零非法动作、零 NaN；Actor/Critic 参数量 ∈ [1e3, 1e5] |

---

## 2. 对照矩阵与臂命名

三开关 (D,E,C) = (`--use-dcca`, `--use-eaps`, `--use-casp`)，八配置全因子（论文口径 c1–c8 见篇首定义表）× 3 训练种子（seed 0/1/2）= 24 臂，全部从头训练、同档同超参、八配置间除三开关外逐字节一致（R3/R4；种子间仅 `--seed` 与输出目录不同）：

| Config | 臂名 | (D,E,C) | 角色 |
|---|---|---|---|
| c1 | `b0_000` | (0,0,0) | **基础 MAPPO（MAPPO⁰）**（全量对照组） |
| c2 | `only_001` | (0,0,1) | 单开 CASP |
| c3 | `only_010` | (0,1,0) | 单开 EAPS |
| c4 | `ad_011` | (0,1,1) | 留一缺 DCCA（c8 的 DCCA 条件贡献对照） |
| c5 | `b1_100` | (1,0,0) | 递增路径第二级 |
| c6 | `ae_101` | (1,0,1) | 留一缺 EAPS |
| c7 | `b2_110` | (1,1,0) | 留一缺 CASP |
| c8 | `b3_111` | (1,1,1) | **EC-MAPPO 全量**（论文框架） |

开关–组件影响（已由 35 项静态审计 + v5 B.3/B.4 证实，本轮仅复验）：

| 开关 | 开(=1) | 关(=0) | 全臂恒定 |
|---|---|---|---|
| `--use-dcca` | credit 注入个体优势；`CriticNet(s,a)` 联合价值 + hold 反事实差 | credit 不注入；`StateCritic` V(s)；个体优势=团队 GAE | 公共动作接口、可行性掩码、基础事件奖励、Actor 执行路径 |
| `--use-eaps` | `R_shaped = R_team + phi_sign·(γΦ_{t+1}−Φ_t)` | Φ 构建跳过，`R_shaped = R_team.clone()` | 数据协议、信用构造 |
| `--use-casp` | x∈R¹⁰（λ̂/n̂ 第 6、9 列）、`MarlNet` 集合注意力 | `drop_m1=True` x∈R⁸、`PoolMLPNet` | Critic 构造、奖励/优势口径 |

---

## 3. 预算档位与阶梯（R8）

- **档 A（首选，= e20/v3 主表同款）**：`--iters 3000 --episodes-per-iter 128 --patience 60 --eval-every 25`（上限 384,000 episodes/臂；e20 实测 1750 iters 早停、0.65h、CPU）。
- **合规窗口**：单臂全量外推墙钟 ∈ [0.5h, 2.0h]；单种子八臂墙钟预期 4.5–9h，本轮 24 臂总墙钟预期 13.5–27h（跨夜批次，`run_e49_batch.sh` 幂等支持中断续跑）。
- **阶梯**：若 (1,1,1) 或 (0,0,0) 50-iter 定档冒烟外推超 2.0h → 降 A−1（`2000×128，patience 45`）→ 仍超 → A−2（`1500×128，patience 40`）。速率保护阈值：外推 (1,1,1)/(0,0,0) 墙钟比 > 2× 时停下报告（不擅自改超参）。
- **降档申报**：任何降档须在报告 §1 与 `logs/ablation_v3_summary.json.budget_tier` 声明，并注明「与 v3 主表 e20 档不可直接对比」；若锁定档 A，报告中注明「档位与主表一致，但 b3_111 各种子为从头训练 + RNG 上下文不同，不要求复现 0.5963」（§0.1 口径）。
- **冻结缺省（R7）**：`--credit-mode=credit_kill`、`--phi-sign=-1.0`、`--c-invalid=None`（=0.01）、`--anneal-iters=2000`、批采样缺省开、`--wall-limit=24.0`、`--device=auto`；`--init-from/--init-blend/--resume/--resume-from` 全程禁止（R3）。八臂命令除三开关外**逐字节一致**。
- 例外：S2/S3 的验收与定档冒烟允许缩 `--iters/--eval-every`，但不得进入八臂正式产物。

---

## 4. 阶段任务

### S1 只读核查（门禁，预期零改动）

1. `git status` 确认 §1 管线文件相对 v5 验收基线无未预期改动；若脏 → 停止并上报用户，不得自行续跑。
2. 复算 `data/dn-data-v3/MANIFEST.md` 全部 30 个实例 md5 与 MANIFEST 表一致；抽查 test 两实例口径（m=3、n=50、K=10、μ=6、总价值 3942/3819）。
3. 确认 `marl/train.py` 缺省 DATA_DIR 即 `data/dn-data-v3`、评估器缺省 manifest 同路径（缺省虽对，命令仍显式传，R6 式严谨）。
4. 确认 `output/` 无 e49/e50/e51 命名冲突。

### S2 v3 范围复验（引用 + 轻量重证，替代重跑 B.3/B.4）

1. **静态审计**：`$PY -m pytest tests/test_ablation_switches.py -q` → 35/35 通过。
2. **(1,1,1) 确定性双跑**：同 seed 0、`--iters 25 --eval-every 5`（其余缺省）跑两次至临时目录，`train_log.jsonl` 剔除 `elapsed/wall` 类字段后**逐位一致**，`best.pt` 学习状态位级一致（差异仅时间戳与输出路径键）。
3. **(0,0,0) MAPPO⁰ 运行时审计**（200 iters，`--eval-every 25`，train 实例）：
   - 埋点性质：全程 `adv_indiv_over_team_std == 1.000`、塑形/基础 `reward_nonzero_ratio` 逐 eval 点相等（EAPS 关语义）；checkpoint `ablation == {0,0,0}`、`actor_type == pool_mlp`、`feature_spec.drop_m1 == True`；
   - 训练健康：零 NaN、零非法动作、shots ≤ 18、ammo_end ≥ 0、val 泄漏率较 iter-0 明显下降（学习成立；阈值参照 e14 首代 0.89→本轮预期 ≤0.85 量级，仅作健康度判据非达标线）；
   - **禁止**与 `e33_v4_mappo`/`e43~e45_v5_mappo*` 或任何历史 MAPPO 数值比对（R1）。
4. **评估器元数据链路**：用 3 的小 checkpoint 跑 `--policy ecmappo --seeds 3`（train split，临时目录），确认日志含还原的结构信息且元数据冲突时报错路径可达（`marl/policy.py` 冲突 raise）。

S2 产物写入 `output/_pre_ablation_ref/v3_recheck_report.md`（含全部判定与命令回显）。

### S3 定档冒烟

(1,1,1) 与 (0,0,0) 各跑 50 iters（其余档 A 参数），记录 s/iter，外推全量墙钟；按 §3 窗口与阶梯锁定档位，写入 `logs/ablation_v3_budget.json`（含定档链：测量值、外推、窗口判定、锁定档）。

### S4 训练批（e49）

- 脚本 `scripts/run_e49_batch.sh`（镜像 v5 `run_e46_batch.sh`：`set -e`、幂等跳过 `last.pt 存在且 log 含 "training done"`、逐臂起止时间戳），覆盖 24 臂 = 8 配置 × 3 种子。
- 执行序（种子外层分三遍，保留风险检查点）：**s0 八臂全跑 → s0 轻量检查点（逐臂自检 + c8↔c1 主指标方向 sanity；异常即停止上报，通过后续跑 s1/s2）→ s1 八臂 → s2 八臂**。
- 配置臂序（含论文口径编号，每个种子遍内相同）：`c1/b0_000 → c8/b3_111 → c4/ad_011 → c6/ae_101 → c7/b2_110 → c5/b1_100 → c3/only_010 → c2/only_001`（先两端与三个留一，再递增/单开补全）。
- 单臂命令模板（`<s>` ∈ {0,1,2}）：

```bash
$PY marl/train.py --data-dir data/dn-data-v3 --seed <s> \
  --iters <档A> --episodes-per-iter 128 --patience 60 --eval-every 25 \
  --use-dcca <d> --use-eaps <e> --use-casp <c> \
  --output output/e49_v3_ablation/<臂名>_s<s> > logs/e49_<臂名>_s<s>.log 2>&1
```

- 逐臂自检（脚本内 `tail` + 事后汇总）：首行 `[ablation] use_dcca=.. use_eaps=.. use_casp=..` 与臂配置一致；`n_agents==3`；checkpoint 元数据三开关与臂一致（best.pt 与 last.pt 均查）；零 NaN/非法；shots ≤ 18；参数量窗口；墙钟记录。
- 训练曲线里程碑（供报告附录，逐臂逐种子记录）：**首个 `val_leak_mean < 0.75` 的 iter**（v3 量级适配：e20 val best 0.6142、e16 0.6144，v5 轮的 0.6 阈值不适用于 v3）+ `best_val@iter` + 终点 val；若某臂某种子 3000 iters 未达 0.75，如实记「未达标」并附 best_val。

### S5 终评 + 泛化（e50）

脚本 `scripts/run_e50_batch.sh`（镜像 v5 `run_e47_batch.sh`，幂等），覆盖 24 臂（8 配置 × 3 种子）。逐臂（`<s>` ∈ {0,1,2}）：

```bash
# (a) 正式终评：test × 30 seeds，含逐步 CPLEX 参考解（gap 口径）
$PY experiments/dn_family_eval.py --split test --policy ecmappo \
  --model output/e49_v3_ablation/<臂名>_s<s>/best.pt --seeds 30 --seed-base 42 \
  --data-dir data/dn-data-v3 --manifest data/dn-data-v3/MANIFEST.md \
  --output output/e50_v3_ablation_<臂名>_s<s>_eval > logs/e50_<臂名>_s<s>.log 2>&1

# (b) 泛化：train/val/test × 30 seeds，--no-ref
... --split <sp> --no-ref --output output/e50_v3_ablation_<臂名>_s<s>_gen/<sp> ...
```

- R5：test split 仅在本阶段使用（S2/S3 一律 train 实例；训练期早停用 val 属管线固有协议，允许）。
- 逐臂自检：`family_report.json` 存在且含 60 runs；泄漏率 ∈ (0,1] 合理区间；shots ≤ 18、ammo ≥ 0；`repeat_targeting_per_run_mean` 与纯策略前向时延（µs）被记录（机制表用）。
- **可复现自检**：c8（`b3_111`）s0 的 test 终评全量复跑一次，两实例 `result_hash` 逐位一致（剔除 wall_time 类字段后）；其余臂/种子抽查 checkpoint 元数据即可。
- **CPLEX 参考解复用**：参考解只依赖 (实例, MC seed) 与 policy 无关；先查 v5 `run_e47_batch.sh` 是否已有跨臂复用/缓存机制，若无则在 e50 脚本中为 24 次终评增加「首算落盘、后续软链/复用」逻辑（仅新增脚本内实现，不改 `dn_family_eval.py`）。
- 运维注（承 output/README）：CPLEX 参考解长跑曾受工作区安全删除阈值影响，若中断，改 `--output /tmp/<同名目录>` 跑完拷回。

### S6 汇总统计（e51）

新增 `experiments/e51_summary.py`（适配自 `e48_summary.py`：E46/E47 路径 → `output/e49_v3_ablation` / `output/e50_v3_ablation_*`；输出 `logs/ablation_v3_summary.json`；收敛里程碑阈值改 0.75；弹药界 18；各臂记录同时含 `config`（"c1"–"c8"）、`arm`、`seed` 三字段；`budget_tier` 承 `logs/ablation_v3_budget.json`；**按种子维度聚合**：主表合并 3 种子 180 runs，另产跨种子 std）。产出：

1. **消融主表（论文口径）**：行 = c1–c8（附臂名括注），列 = {①泄漏率 mean±std、④命中率、⑰无效交战率、期望摧毁价值、单位弹药摧毁价值、worst、Actor/Critic 参数量、纯策略前向时延}（test，**每臂合并 3 种子 × 2 实例 × 30 MC = 180 runs**）；c8 行加粗，表式见 S7 主表模板。
2. **机制指标表**：credit 非零占比、优势区分度（DCCA 关时恒 1.000 的结构断言）、奖励密度（塑形/基础）、收敛里程碑（首个 val<0.75 的 iter）、best_val@iter、重复瞄准均值。
3. **配对比较**（主指标 test 泄漏率；**配对单元 = 2 实例 × 30 MC seeds × 3 训练种子 = 180 配对**，同 (实例, MC seed, 训练种子) 内跨配置配对）：四组核心对 {整体 c8↔c1；DCCA c8↔c4；EAPS c8↔c6；CASP c8↔c7}：均值差、配对 t 95% CI、bootstrap CI、Wilcoxon p（α=0.05）。
4. **八配置描述性分析**：单元格均值（合并 3 种子）、主效应（开−关）、两两交互（重点 D×E；仅描述性，辅以跨种子方向一致性）；递增路径 c1→c5→c7→c8 的单调性描述。
5. **泛化表**：c1–c8 × {train/val/test} 泄漏率均值（合并 3 种子），跨 split 排序一致性。
6. **跨种子稳定性表（附录）**：每臂 3 个种子的 60-run 均值逐行列出 + 跨种子 mean±std（纯训练方差口径），并给出主指标跨种子方向一致性（如 c8<c1 是否三种子同向）。
7. **跨家族对照（描述性）**：与 v5 轮（档 A−1）八配置同位数字按 c1–c8 对齐并列成表，仅讨论方向一致性（如 DCCA 条件贡献、D×E 交互方向），两轮档位不同禁止数值直比（R8）。

### S7 报告与红线

产出《实验报告_v3消融.md》，骨架镜像 v5 报告：§1 预算档位声明（含 §0.1 口径申明）→ §2 消融主表（论文口径，见下模板）+ 泛化表 → §3 机制指标表 → §4 配对比较 → §5 结论（按界定文档 §7.2 以 3 训练种子作**正式结论**，附跨种子方向一致性）→ §6 附录（c1–c8 配置定义表、开关-组件对照、S2 复验记录摘要、八配置训练曲线里程碑、**跨种子稳定性表**、R7 冻结记录、R1–R9 自查表、交付物索引）。

**§2 消融主表模板**（行 = Ablation config.，列与《实验报告_v3全算法总览》主表逐列一致，c8 行加粗，可与总览表并排直接对照；表值由 `e51_summary.py` 从 `family_report.json` 生成填充，扩展列 worst/单位弹药摧毁价值/参数量按需另列附录表）：

| Ablation config. | ①泄漏率↓ (mean±std) | ④命中率↑（命中/发射） | ⑰无效交战率↓ | 期望摧毁价值↑ | ⑦时延 P50/P90 (s)↓ |
|---|---|---|---|---|---|
| c1 (MAPPO⁰) | … | … | … | … | … |
| c2 (+CASP) | … | … | … | … | … |
| c3 (+EAPS) | … | … | … | … | … |
| c4 (EAPS+CASP) | … | … | … | … | … |
| c5 (+DCCA) | … | … | … | … | … |
| c6 (DCCA+CASP) | … | … | … | … | … |
| c7 (DCCA+EAPS) | … | … | … | … | … |
| **c8 (EC-MAPPO full)** | … | … | … | … | … |

（示例参照：总览表 EC-MAPPO 行 = 0.5963±0.0168 / 83.2% / 0.1095 / 1565.4 / 0.41/0.45，c8 行落在同量级邻域即符合 §0.1 预期。**主表口径注（必须随表申明）**：消融表 mean±std 合并 3 训练种子 × 2 实例 × 30 MC = 180 runs，std 同时含训练/实例/MC 三重方差，与总览表（单种子 60 runs）并排对照时口径差异须括注；纯训练方差见附录跨种子稳定性表。）

**R1–R9（v3 版）**：

| # | 红线 | 本轮锚点 |
|---|---|---|
| R1 | 历史 MAPPO 隔离 | 不引用 `train_mappo.py`（不存在）/e33_v4_mappo/e43~e45_v5_mappo* 任何产物 |
| R2 | 统一宿主 | 八臂均由 `marl/train.py` 训练，无独立脚本 |
| R3 | 从头训练 | 无 init/resume/blend |
| R4 | 同档同超参 | 八臂仅三开关不同，逐字节一致（脚本可 diff 佐证） |
| R5 | test 隔离 | 仅 e50 触 test；冒烟/验收用 train |
| R6 | 数据纯净 | 仅 `data/dn-data-v3`，命令显式传 data-dir/manifest |
| R7 | 冻结缺省 | §3 冻结清单，报告附实际命令 diff |
| R8 | 档位申报 | 与 v3 主表 e20 档、v5 消融 A−1 档的关系均显式声明，不混排 |
| R9 | 维度推导 | n_agents=3 由实例推导，断言 checkpoint 元数据；禁用 legacy N_AGENTS |

**解释红线**：结论按界定文档 §7.2 以 3 训练种子作正式表述，主指标结论须附跨种子方向一致性（至少 2/3 种子同向才写方向性结论，否则如实报告分歧）；三个留一差值不可相加为「总提升」；非单调路径优先归因训练方差与交互，不写正贡献；CASP 规模适应性在 v3 家族（同 3×50）内仍无法检验，留待跨家族评估。

### S8 交付物清单与验收顺序

```
marl/ 与 dwta/ 管线零改动（git diff 空）
scripts/run_e49_batch.sh  scripts/run_e50_batch.sh
experiments/e51_summary.py
output/e49_v3_ablation/<八臂>_s{0,1,2}/{best.pt,last.pt,train_log.jsonl,train_summary.json}（24 臂）
output/e50_v3_ablation_<臂>_s{0,1,2}_eval/ 与 ..._s{0,1,2}_gen/{train,val,test}/
output/_pre_ablation_ref/v3_recheck_report.md
logs/ablation_v3_budget.json  logs/ablation_v3_summary.json
logs/e49_*.log  logs/e50_*.log
实验报告_v3消融.md
```

执行顺序：S1 门禁 → S2 复验 → S3 定档 → S4 训练（可中断续跑）→ S5 评估 → S6 汇总 → S7 报告；S1/S2 任一失败即停止上报，不得带病进入 S4。

---

## 5. 种子维度说明与非目标

- **种子维度（已并入本轮）**：原设计的「阶段 2 补种子 s1/s2」已合并进本轮一次性执行（§0/§S4–S6：24 臂、180-run 主表、附录跨种子表）；`e51_summary.py` 自始即按种子维度实现，目录/命名按 `<臂>_s{0,1,2}` 约定，后续若需扩种子（s3+）仅扩批不改编。
- **非目标**：不修任何算法/环境代码；不做参数量匹配对照臂；不做跨数据集混合训练；不做 v4；不改主表历史数字或重评 e20 checkpoint（其格式为前消融元数据格式，评估器会拒绝加载，属预期行为，不修）。
